{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Kubernetes Lite","text":"<p>Kubernetes Lite is a \"liteweight\" wrapper around various golang Kubernetes libraries using gopy. It brings the benefits of go's resource efficiency plus the feature set of packages like client-go and the controller-runtime to Python.</p> <p>For API details, please refer to the API References.</p>"},{"location":"api_references/","title":"API References","text":"<p>Kubernetes_lite is a lightweight wrapper around the dynamic client from k8s.io/client-go/dynamic, the 'mock' kubernetes server from sigs.k8s.io/controller-runtime/pkg/envtest, and its helper tool setup-envtest https://pkg.go.dev/sigs.k8s.io/controller-runtime/tools/setup-envtest</p>"},{"location":"api_references/#kubernetes_lite.client","title":"<code>client</code>","text":"<p>This module contains a simple pythonic wrapper around the k8s.io/client-go/dynamic DynamicClient.</p>"},{"location":"api_references/#kubernetes_lite.client.DynamicClient","title":"<code>DynamicClient</code>","text":"<p>DynamicClient is the top level client and generic controls settings like authentication, rate limiting, and timeouts. Use the .resource method to create a DynamicResource which can be used to interact with the cluster.</p> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>class DynamicClient:\n    \"\"\"DynamicClient is the top level client and generic controls settings like authentication,\n    rate limiting, and timeouts. Use the .resource method to create a DynamicResource which\n    can be used to interact with the cluster.\n    \"\"\"\n\n    def __init__(\n        self,\n        config: bytes | BytesIO | None = None,\n        qps: float = -1,\n        burst: int = 100,\n        timeout: datetime.timedelta | str = \"\",\n        **kwargs,\n    ):\n        \"\"\"Initialize the dynamic client with optional overrides\n\n        Args:\n            config (bytes | BytesIO | None, optional): The raw kube_config data to initialize the client with. Defaults\n                to controller-runtime's GetConfig.\n                See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.\n            qps (float, optional): QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n                for more information. Defaults to -1 which means rate-limiting is disabled.\n            burst (int, optional): How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n                for more information. Defaults to 100.\n            timeout (datetime.timedelta | str, optional): Timeout value for the client. If a string is provided check\n                https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.\n            **kwargs: Unused kwargs for backwards compatibility with kubernetes library\n        \"\"\"\n        # Parse timeout into a string\n        if isinstance(timeout, datetime.timedelta):\n            timeout = f\"{timeout.total_seconds()}s\"\n\n        # Setup the common client arguments\n        client_args = [qps, burst, timeout]\n        if config:\n            self.client = NewWrappedDynamicClientWithConfig(serialize_to_go(config), *client_args)\n        else:\n            self.client = NewWrappedDynamicClient(*client_args)\n\n    @cached_property\n    def resources(self) -&gt; Discoverer:\n        \"\"\"Return the client's Discover object. It's preferred to use the resource()\n        function instead of accessing this directly\n\n        Returns:\n            Discoverer: The discover used in created DynamicResources\n        \"\"\"\n        return Discoverer(self.client)\n\n    def resource(self, api_version: str, kind: str) -&gt; DynamicResource:\n        \"\"\"Return a DynamicResource for the given api_version and kind. This\n        DynamicResource can then be used to interact with the cluster.\n\n        Args:\n            api_version (str): The api_version for the resource handle\n            kind (str): The kind for the resource handle\n\n        Returns:\n            DynamicResource: The DynamicResource handle for the requested type\n        \"\"\"\n        return self.resources.get(api_version, kind)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicClient.resources","title":"<code>resources</code>  <code>cached</code> <code>property</code>","text":"<p>Return the client's Discover object. It's preferred to use the resource() function instead of accessing this directly</p> <p>Returns:</p> Name Type Description <code>Discoverer</code> <code>Discoverer</code> <p>The discover used in created DynamicResources</p>"},{"location":"api_references/#kubernetes_lite.client.DynamicClient.__init__","title":"<code>__init__(config=None, qps=-1, burst=100, timeout='', **kwargs)</code>","text":"<p>Initialize the dynamic client with optional overrides</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>bytes | BytesIO | None</code> <p>The raw kube_config data to initialize the client with. Defaults to controller-runtime's GetConfig. See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.</p> <code>None</code> <code>qps</code> <code>float</code> <p>QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config for more information. Defaults to -1 which means rate-limiting is disabled.</p> <code>-1</code> <code>burst</code> <code>int</code> <p>How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config for more information. Defaults to 100.</p> <code>100</code> <code>timeout</code> <code>timedelta | str</code> <p>Timeout value for the client. If a string is provided check https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.</p> <code>''</code> <code>**kwargs</code> <p>Unused kwargs for backwards compatibility with kubernetes library</p> <code>{}</code> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>def __init__(\n    self,\n    config: bytes | BytesIO | None = None,\n    qps: float = -1,\n    burst: int = 100,\n    timeout: datetime.timedelta | str = \"\",\n    **kwargs,\n):\n    \"\"\"Initialize the dynamic client with optional overrides\n\n    Args:\n        config (bytes | BytesIO | None, optional): The raw kube_config data to initialize the client with. Defaults\n            to controller-runtime's GetConfig.\n            See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.\n        qps (float, optional): QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n            for more information. Defaults to -1 which means rate-limiting is disabled.\n        burst (int, optional): How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n            for more information. Defaults to 100.\n        timeout (datetime.timedelta | str, optional): Timeout value for the client. If a string is provided check\n            https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.\n        **kwargs: Unused kwargs for backwards compatibility with kubernetes library\n    \"\"\"\n    # Parse timeout into a string\n    if isinstance(timeout, datetime.timedelta):\n        timeout = f\"{timeout.total_seconds()}s\"\n\n    # Setup the common client arguments\n    client_args = [qps, burst, timeout]\n    if config:\n        self.client = NewWrappedDynamicClientWithConfig(serialize_to_go(config), *client_args)\n    else:\n        self.client = NewWrappedDynamicClient(*client_args)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicClient.resource","title":"<code>resource(api_version, kind)</code>","text":"<p>Return a DynamicResource for the given api_version and kind. This DynamicResource can then be used to interact with the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>api_version</code> <code>str</code> <p>The api_version for the resource handle</p> required <code>kind</code> <code>str</code> <p>The kind for the resource handle</p> required <p>Returns:</p> Name Type Description <code>DynamicResource</code> <code>DynamicResource</code> <p>The DynamicResource handle for the requested type</p> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>def resource(self, api_version: str, kind: str) -&gt; DynamicResource:\n    \"\"\"Return a DynamicResource for the given api_version and kind. This\n    DynamicResource can then be used to interact with the cluster.\n\n    Args:\n        api_version (str): The api_version for the resource handle\n        kind (str): The kind for the resource handle\n\n    Returns:\n        DynamicResource: The DynamicResource handle for the requested type\n    \"\"\"\n    return self.resources.get(api_version, kind)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource","title":"<code>DynamicResource</code>","text":"<p>DynamicResource is the primary client for interacting with resources on the cluster. It's functions mirror the ResourceInterface provided by https://pkg.go.dev/k8s.io/client-go/dynamic#ResourceInterface. The parameters to the various functions map to the json names of the Options objects in https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1. For example <code>list()</code> parameter's map to ListOptions</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>class DynamicResource:\n    \"\"\"DynamicResource is the primary client for interacting with resources on the cluster. It's\n    functions mirror the ResourceInterface provided by https://pkg.go.dev/k8s.io/client-go/dynamic#ResourceInterface.\n    The parameters to the various functions map to the json names of the Options objects in\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1. For example `list()` parameter's map\n    to ListOptions\n    \"\"\"\n\n    def __init__(self, resource: WrappedNamespaceableResourceInterface):\n        \"\"\"Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class\n        should not be initialized directly and should instead be created by the client/discoverer\n\n        Args:\n            resource (WrappedNamespaceableResourceInterface): The WrappedNamespaceableResourceInterface to wrap\n        \"\"\"\n        self._resource = resource\n\n    @wrap_kube_error\n    def get(\n        self,\n        name: str | None = None,\n        namespace: str | None = None,\n        resource_version: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions\n\n        Args:\n            name (str | None, optional): The name of the resource to fetch. **None is Deprecated**. Defaults to None.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more GetOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The object from the cluster as a dictionary\n        \"\"\"\n        if name is None:\n            warnings.warn(\n                \"Get with no name to list objects is deprecated. Use the list method instead\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            return self.list(namespace=namespace, resource_version=resource_version, **kwargs)\n\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Get(name, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def create(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The created object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Create(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def update(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The updated object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Update(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def update_status(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The updated object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.UpdateStatus(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def patch(  # noqa: PLR0917, PLR0913\n        self,\n        name: str,\n        patch_type: str,\n        patch_data: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions\n\n        Args:\n            name (str): Name of the object to patch\n            patch_type (str): Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for\n                available options\n            patch_data (SourceObjectType): The raw patch data to use\n\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more PatchOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The patched object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        if field_manager:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_patch = serialize_to_go(patch_data)\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Patch(name, patch_type, serialized_patch, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def apply(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        name: str | None = None,\n        namespace: str | None = None,\n        dry_run: bool | None = None,\n        field_manager: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n        See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            name (str | None, optional): Optional name for the resource. Defaults to None or name\n                in the provided resources.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The applied object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if force:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        if not name and resource_metadata.name:\n            name = resource_metadata.name\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Apply(name, serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def apply_status(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        name: str | None = None,\n        namespace: str | None = None,\n        dry_run: bool | None = None,\n        field_manager: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n        See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            name (str | None, optional): Optional name for the resource. Defaults to None or name\n                in the provided resources.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The applied object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if force:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        if not name and resource_metadata.name:\n            name = resource_metadata.name\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.ApplyStatus(name, serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def delete(  # noqa: PLR0917, PLR0913\n        self,\n        name: str,\n        namespace: str | None = None,\n        grace_period_seconds: int | None = None,\n        propagation_policy: str | None = None,\n        dry_run: list[str] | None = None,\n        ignore_store_read_error: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ):\n        \"\"\"Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.\n\n        Args:\n            name (str): Name for the resource to delete.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n                deleting the resource. Defaults to None.\n            propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n                to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n                for more information. Defaults to None.\n            ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n                if there is an etcd store error. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more DeleteOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if grace_period_seconds:\n            options[\"gracePeriodSeconds\"] = grace_period_seconds\n        if propagation_policy:\n            options[\"PropagationPolicy\"] = propagation_policy\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if ignore_store_read_error:\n            options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        resource_ptr.Delete(name, serialized_options)\n\n    @wrap_kube_error\n    def delete_collection(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        grace_period_seconds: int | None = None,\n        propagation_policy: str | None = None,\n        dry_run: list[str] | None = None,\n        limit: int | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        ignore_store_read_error: bool | None = None,\n        extra_delete_options_kwargs: dict[str, Any] | None = None,\n        extra_list_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ):\n        \"\"\"Delete a collection of objects from the cluster. The parameters map to both\n        https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and\n         https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n        Args:\n            name (str): Name for the resource to delete.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n                deleting the resource. Defaults to None.\n            propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n                to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n                for more information. Defaults to None.\n            ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n                if there is an etcd store error. Defaults to None.\n            extra_delete_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more\n                DeleteOptions parameters. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            extra_list_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n        \"\"\"\n        delete_options = {**(extra_delete_options_kwargs or {}), **kwargs}\n        if grace_period_seconds:\n            delete_options[\"gracePeriodSeconds\"] = grace_period_seconds\n        if propagation_policy:\n            delete_options[\"PropagationPolicy\"] = propagation_policy\n        if dry_run:\n            delete_options[\"dryRun\"] = dry_run\n        if ignore_store_read_error:\n            delete_options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n\n        list_options = {**(extra_list_options_kwargs or {}), **kwargs}\n        if resource_version:\n            list_options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            list_options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            list_options[\"labelSelector\"] = label_selector\n        if field_selector:\n            list_options[\"fieldSelector\"] = field_selector\n        if timeout:\n            list_options[\"timeoutSeconds\"] = timeout\n        if limit:\n            list_options[\"Limit\"] = limit\n        serialized_del_options = serialize_to_go(delete_options)\n        serialized_list_options = serialize_to_go(list_options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n        resource_ptr.DeleteCollection(serialized_del_options, serialized_list_options)\n\n    @wrap_kube_error\n    def watch(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        limit: int | None = None,\n        continue_req: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        watcher: Watch | None = None,\n        **kwargs,\n    ) -&gt; Watch:\n        \"\"\"Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions\n\n        Args:\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n                limit keywarg. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n            watcher (Watch | None, optional): Optional Watch object to use as the watcher interface. Users\n                can use this to stop the event stream during or outside of iteration\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n                resources\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            options[\"labelSelector\"] = label_selector\n        if field_selector:\n            options[\"fieldSelector\"] = field_selector\n        if timeout:\n            options[\"timeoutSeconds\"] = timeout\n        if limit:\n            options[\"Limit\"] = limit\n        if continue_req:\n            options[\"continue\"] = continue_req\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        if not watcher:\n            watcher = Watch()\n\n        interface = resource_ptr.Watch(serialized_options)\n        watcher.interface = interface\n\n        return watcher\n\n    # ! Defined at bottom of class to avoid overriding built-in list\n    @wrap_kube_error\n    def list(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        limit: int | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        continue_req: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n        Args:\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n                limit keywarg. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n                resources\n        \"\"\"\n        options = {\n            **(extra_options_kwargs or {}),\n            **kwargs,\n        }\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            options[\"labelSelector\"] = label_selector\n        if field_selector:\n            options[\"fieldSelector\"] = field_selector\n        if timeout:\n            options[\"timeoutSeconds\"] = timeout\n        if limit:\n            options[\"Limit\"] = limit\n        if continue_req:\n            options[\"continue\"] = continue_req\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n        result = resource_ptr.List(serialized_options)\n        return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.__init__","title":"<code>__init__(resource)</code>","text":"<p>Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class should not be initialized directly and should instead be created by the client/discoverer</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>WrappedNamespaceableResourceInterface</code> <p>The WrappedNamespaceableResourceInterface to wrap</p> required Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>def __init__(self, resource: WrappedNamespaceableResourceInterface):\n    \"\"\"Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class\n    should not be initialized directly and should instead be created by the client/discoverer\n\n    Args:\n        resource (WrappedNamespaceableResourceInterface): The WrappedNamespaceableResourceInterface to wrap\n    \"\"\"\n    self._resource = resource\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.apply","title":"<code>apply(resource, name=None, namespace=None, dry_run=None, field_manager=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions. See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>name</code> <code>str | None</code> <p>Optional name for the resource. Defaults to None or name in the provided resources.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ApplyOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The applied object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef apply(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    name: str | None = None,\n    namespace: str | None = None,\n    dry_run: bool | None = None,\n    field_manager: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n    See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        name (str | None, optional): Optional name for the resource. Defaults to None or name\n            in the provided resources.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The applied object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if force:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    if not name and resource_metadata.name:\n        name = resource_metadata.name\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Apply(name, serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.apply_status","title":"<code>apply_status(resource, name=None, namespace=None, dry_run=None, field_manager=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions. See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>name</code> <code>str | None</code> <p>Optional name for the resource. Defaults to None or name in the provided resources.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> required <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ApplyOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The applied object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef apply_status(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    name: str | None = None,\n    namespace: str | None = None,\n    dry_run: bool | None = None,\n    field_manager: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n    See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        name (str | None, optional): Optional name for the resource. Defaults to None or name\n            in the provided resources.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The applied object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if force:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    if not name and resource_metadata.name:\n        name = resource_metadata.name\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.ApplyStatus(name, serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.create","title":"<code>create(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The created object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef create(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The created object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Create(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.delete","title":"<code>delete(name, namespace=None, grace_period_seconds=None, propagation_policy=None, dry_run=None, ignore_store_read_error=None, extra_options_kwargs=None, **kwargs)</code>","text":"<p>Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the resource to delete.</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>grace_period_seconds</code> <code>int | None</code> <p>Optional time to wait for graceful termination before deleting the resource. Defaults to None.</p> <code>None</code> <code>propagation_policy</code> <code>str | None</code> <p>Optional DeletionPropagation decides how a deletion propagates to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation for more information. Defaults to None.</p> <code>None</code> <code>ignore_store_read_error</code> <code>bool | None</code> <p>Optional override to force successful deletion even if there is an etcd store error. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more DeleteOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> required <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef delete(  # noqa: PLR0917, PLR0913\n    self,\n    name: str,\n    namespace: str | None = None,\n    grace_period_seconds: int | None = None,\n    propagation_policy: str | None = None,\n    dry_run: list[str] | None = None,\n    ignore_store_read_error: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n):\n    \"\"\"Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.\n\n    Args:\n        name (str): Name for the resource to delete.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n            deleting the resource. Defaults to None.\n        propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n            to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n            for more information. Defaults to None.\n        ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n            if there is an etcd store error. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more DeleteOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if grace_period_seconds:\n        options[\"gracePeriodSeconds\"] = grace_period_seconds\n    if propagation_policy:\n        options[\"PropagationPolicy\"] = propagation_policy\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if ignore_store_read_error:\n        options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    resource_ptr.Delete(name, serialized_options)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.delete_collection","title":"<code>delete_collection(namespace=None, grace_period_seconds=None, propagation_policy=None, dry_run=None, limit=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, ignore_store_read_error=None, extra_delete_options_kwargs=None, extra_list_options_kwargs=None, **kwargs)</code>","text":"<p>Delete a collection of objects from the cluster. The parameters map to both https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and  https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the resource to delete.</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>grace_period_seconds</code> <code>int | None</code> <p>Optional time to wait for graceful termination before deleting the resource. Defaults to None.</p> <code>None</code> <code>propagation_policy</code> <code>str | None</code> <p>Optional DeletionPropagation decides how a deletion propagates to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation for more information. Defaults to None.</p> <code>None</code> <code>ignore_store_read_error</code> <code>bool | None</code> <p>Optional override to force successful deletion even if there is an etcd store error. Defaults to None.</p> <code>None</code> <code>extra_delete_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more DeleteOptions parameters. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>extra_list_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef delete_collection(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    grace_period_seconds: int | None = None,\n    propagation_policy: str | None = None,\n    dry_run: list[str] | None = None,\n    limit: int | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    ignore_store_read_error: bool | None = None,\n    extra_delete_options_kwargs: dict[str, Any] | None = None,\n    extra_list_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n):\n    \"\"\"Delete a collection of objects from the cluster. The parameters map to both\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and\n     https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n    Args:\n        name (str): Name for the resource to delete.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n            deleting the resource. Defaults to None.\n        propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n            to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n            for more information. Defaults to None.\n        ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n            if there is an etcd store error. Defaults to None.\n        extra_delete_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more\n            DeleteOptions parameters. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        extra_list_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n    \"\"\"\n    delete_options = {**(extra_delete_options_kwargs or {}), **kwargs}\n    if grace_period_seconds:\n        delete_options[\"gracePeriodSeconds\"] = grace_period_seconds\n    if propagation_policy:\n        delete_options[\"PropagationPolicy\"] = propagation_policy\n    if dry_run:\n        delete_options[\"dryRun\"] = dry_run\n    if ignore_store_read_error:\n        delete_options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n\n    list_options = {**(extra_list_options_kwargs or {}), **kwargs}\n    if resource_version:\n        list_options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        list_options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        list_options[\"labelSelector\"] = label_selector\n    if field_selector:\n        list_options[\"fieldSelector\"] = field_selector\n    if timeout:\n        list_options[\"timeoutSeconds\"] = timeout\n    if limit:\n        list_options[\"Limit\"] = limit\n    serialized_del_options = serialize_to_go(delete_options)\n    serialized_list_options = serialize_to_go(list_options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n    resource_ptr.DeleteCollection(serialized_del_options, serialized_list_options)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.get","title":"<code>get(name=None, namespace=None, resource_version=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>The name of the resource to fetch. None is Deprecated. Defaults to None.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more GetOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The object from the cluster as a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef get(\n    self,\n    name: str | None = None,\n    namespace: str | None = None,\n    resource_version: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions\n\n    Args:\n        name (str | None, optional): The name of the resource to fetch. **None is Deprecated**. Defaults to None.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more GetOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The object from the cluster as a dictionary\n    \"\"\"\n    if name is None:\n        warnings.warn(\n            \"Get with no name to list objects is deprecated. Use the list method instead\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        return self.list(namespace=namespace, resource_version=resource_version, **kwargs)\n\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Get(name, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.list","title":"<code>list(namespace=None, limit=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, continue_req=None, extra_options_kwargs=None, **kwargs)</code>","text":"<p>List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>continue_req</code> <code>str | None</code> <p>Optional continue token to use to iterate over more results. Used with limit keywarg. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Optional subresource of an object to get. Defaults to None.</p> required <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The ObjectList object from the cluster as a dictionary e.g. use <code>.get(\"items\")</code> to get list of resources</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef list(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    limit: int | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    continue_req: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n    Args:\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n            limit keywarg. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n            resources\n    \"\"\"\n    options = {\n        **(extra_options_kwargs or {}),\n        **kwargs,\n    }\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        options[\"labelSelector\"] = label_selector\n    if field_selector:\n        options[\"fieldSelector\"] = field_selector\n    if timeout:\n        options[\"timeoutSeconds\"] = timeout\n    if limit:\n        options[\"Limit\"] = limit\n    if continue_req:\n        options[\"continue\"] = continue_req\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n    result = resource_ptr.List(serialized_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.patch","title":"<code>patch(name, patch_type, patch_data, namespace=None, dry_run=None, field_manager=None, field_validation=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the object to patch</p> required <code>patch_type</code> <code>str</code> <p>Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for available options</p> required <code>patch_data</code> <code>SourceObjectType</code> <p>The raw patch data to use</p> required <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more PatchOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The patched object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef patch(  # noqa: PLR0917, PLR0913\n    self,\n    name: str,\n    patch_type: str,\n    patch_data: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions\n\n    Args:\n        name (str): Name of the object to patch\n        patch_type (str): Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for\n            available options\n        patch_data (SourceObjectType): The raw patch data to use\n\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more PatchOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The patched object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    if field_manager:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_patch = serialize_to_go(patch_data)\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Patch(name, patch_type, serialized_patch, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.update","title":"<code>update(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The updated object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef update(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The updated object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Update(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.update_status","title":"<code>update_status(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The updated object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef update_status(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The updated object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.UpdateStatus(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.DynamicResource.watch","title":"<code>watch(namespace=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, limit=None, continue_req=None, extra_options_kwargs=None, watcher=None, **kwargs)</code>","text":"<p>Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>continue_req</code> <code>str | None</code> <p>Optional continue token to use to iterate over more results. Used with limit keywarg. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Optional subresource of an object to get. Defaults to None.</p> required <code>watcher</code> <code>Watch | None</code> <p>Optional Watch object to use as the watcher interface. Users can use this to stop the event stream during or outside of iteration</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>Watch</code> <p>The ObjectList object from the cluster as a dictionary e.g. use <code>.get(\"items\")</code> to get list of resources</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef watch(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    limit: int | None = None,\n    continue_req: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    watcher: Watch | None = None,\n    **kwargs,\n) -&gt; Watch:\n    \"\"\"Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions\n\n    Args:\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n            limit keywarg. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n        watcher (Watch | None, optional): Optional Watch object to use as the watcher interface. Users\n            can use this to stop the event stream during or outside of iteration\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n            resources\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        options[\"labelSelector\"] = label_selector\n    if field_selector:\n        options[\"fieldSelector\"] = field_selector\n    if timeout:\n        options[\"timeoutSeconds\"] = timeout\n    if limit:\n        options[\"Limit\"] = limit\n    if continue_req:\n        options[\"continue\"] = continue_req\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    if not watcher:\n        watcher = Watch()\n\n    interface = resource_ptr.Watch(serialized_options)\n    watcher.interface = interface\n\n    return watcher\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.Watch","title":"<code>Watch</code>","text":"<p>Watch is an iterator that yields events streamed from the cluster</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>class Watch:\n    \"\"\"Watch is an iterator that yields events streamed from\n    the cluster\n    \"\"\"\n\n    interface: WrappedWatchInterface | None = None\n\n    def __init__(self, interface: WrappedWatchInterface | None = None):\n        \"\"\"Initialize the Watch instance with a given wrapped interface\n\n        Args:\n            interface (WrappedWatchInterface | None, optional): The interface to yield events from. Defaults to None.\n        \"\"\"\n        self.interface = interface\n\n    def stop(self):\n        \"\"\"Stop streaming events from the cluster\"\"\"\n        if not self.interface:\n            raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n        self.interface.Stop()\n\n    def __iter__(self) -&gt; \"Watch\":\n        \"\"\"Complete implementation of a python iterator\n\n        Returns:\n            Watch: Itself to iterate\n        \"\"\"\n        return self\n\n    def __next__(self) -&gt; dict:\n        \"\"\"Return the next event from the cluster. These events are dictionaries in the\n        format of json encoded apimachinery Events. You can find more information here:\n        https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event\n\n        Returns:\n            dict: The event data\n        \"\"\"\n        if not self.interface:\n            raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n        # Try to get an event from the underlying interface\n        try:\n            event_data = self.interface.Next()\n        except RuntimeError as exc:\n            if \"stop iteration\" in str(exc):\n                raise StopIteration from None\n\n        # Return the deserialized result\n        return deserialize_result(bytes(event_data))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.Watch.__init__","title":"<code>__init__(interface=None)</code>","text":"<p>Initialize the Watch instance with a given wrapped interface</p> <p>Parameters:</p> Name Type Description Default <code>interface</code> <code>WrappedWatchInterface | None</code> <p>The interface to yield events from. Defaults to None.</p> <code>None</code> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __init__(self, interface: WrappedWatchInterface | None = None):\n    \"\"\"Initialize the Watch instance with a given wrapped interface\n\n    Args:\n        interface (WrappedWatchInterface | None, optional): The interface to yield events from. Defaults to None.\n    \"\"\"\n    self.interface = interface\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.Watch.__iter__","title":"<code>__iter__()</code>","text":"<p>Complete implementation of a python iterator</p> <p>Returns:</p> Name Type Description <code>Watch</code> <code>Watch</code> <p>Itself to iterate</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __iter__(self) -&gt; \"Watch\":\n    \"\"\"Complete implementation of a python iterator\n\n    Returns:\n        Watch: Itself to iterate\n    \"\"\"\n    return self\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.Watch.__next__","title":"<code>__next__()</code>","text":"<p>Return the next event from the cluster. These events are dictionaries in the format of json encoded apimachinery Events. You can find more information here: https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The event data</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __next__(self) -&gt; dict:\n    \"\"\"Return the next event from the cluster. These events are dictionaries in the\n    format of json encoded apimachinery Events. You can find more information here:\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event\n\n    Returns:\n        dict: The event data\n    \"\"\"\n    if not self.interface:\n        raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n    # Try to get an event from the underlying interface\n    try:\n        event_data = self.interface.Next()\n    except RuntimeError as exc:\n        if \"stop iteration\" in str(exc):\n            raise StopIteration from None\n\n    # Return the deserialized result\n    return deserialize_result(bytes(event_data))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.Watch.stop","title":"<code>stop()</code>","text":"<p>Stop streaming events from the cluster</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def stop(self):\n    \"\"\"Stop streaming events from the cluster\"\"\"\n    if not self.interface:\n        raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n    self.interface.Stop()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.client","title":"<code>client</code>","text":"<p>This module contains the implementation of the top level dynamic client</p>"},{"location":"api_references/#kubernetes_lite.client.client.DynamicClient","title":"<code>DynamicClient</code>","text":"<p>DynamicClient is the top level client and generic controls settings like authentication, rate limiting, and timeouts. Use the .resource method to create a DynamicResource which can be used to interact with the cluster.</p> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>class DynamicClient:\n    \"\"\"DynamicClient is the top level client and generic controls settings like authentication,\n    rate limiting, and timeouts. Use the .resource method to create a DynamicResource which\n    can be used to interact with the cluster.\n    \"\"\"\n\n    def __init__(\n        self,\n        config: bytes | BytesIO | None = None,\n        qps: float = -1,\n        burst: int = 100,\n        timeout: datetime.timedelta | str = \"\",\n        **kwargs,\n    ):\n        \"\"\"Initialize the dynamic client with optional overrides\n\n        Args:\n            config (bytes | BytesIO | None, optional): The raw kube_config data to initialize the client with. Defaults\n                to controller-runtime's GetConfig.\n                See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.\n            qps (float, optional): QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n                for more information. Defaults to -1 which means rate-limiting is disabled.\n            burst (int, optional): How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n                for more information. Defaults to 100.\n            timeout (datetime.timedelta | str, optional): Timeout value for the client. If a string is provided check\n                https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.\n            **kwargs: Unused kwargs for backwards compatibility with kubernetes library\n        \"\"\"\n        # Parse timeout into a string\n        if isinstance(timeout, datetime.timedelta):\n            timeout = f\"{timeout.total_seconds()}s\"\n\n        # Setup the common client arguments\n        client_args = [qps, burst, timeout]\n        if config:\n            self.client = NewWrappedDynamicClientWithConfig(serialize_to_go(config), *client_args)\n        else:\n            self.client = NewWrappedDynamicClient(*client_args)\n\n    @cached_property\n    def resources(self) -&gt; Discoverer:\n        \"\"\"Return the client's Discover object. It's preferred to use the resource()\n        function instead of accessing this directly\n\n        Returns:\n            Discoverer: The discover used in created DynamicResources\n        \"\"\"\n        return Discoverer(self.client)\n\n    def resource(self, api_version: str, kind: str) -&gt; DynamicResource:\n        \"\"\"Return a DynamicResource for the given api_version and kind. This\n        DynamicResource can then be used to interact with the cluster.\n\n        Args:\n            api_version (str): The api_version for the resource handle\n            kind (str): The kind for the resource handle\n\n        Returns:\n            DynamicResource: The DynamicResource handle for the requested type\n        \"\"\"\n        return self.resources.get(api_version, kind)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.client.DynamicClient.resources","title":"<code>resources</code>  <code>cached</code> <code>property</code>","text":"<p>Return the client's Discover object. It's preferred to use the resource() function instead of accessing this directly</p> <p>Returns:</p> Name Type Description <code>Discoverer</code> <code>Discoverer</code> <p>The discover used in created DynamicResources</p>"},{"location":"api_references/#kubernetes_lite.client.client.DynamicClient.__init__","title":"<code>__init__(config=None, qps=-1, burst=100, timeout='', **kwargs)</code>","text":"<p>Initialize the dynamic client with optional overrides</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>bytes | BytesIO | None</code> <p>The raw kube_config data to initialize the client with. Defaults to controller-runtime's GetConfig. See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.</p> <code>None</code> <code>qps</code> <code>float</code> <p>QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config for more information. Defaults to -1 which means rate-limiting is disabled.</p> <code>-1</code> <code>burst</code> <code>int</code> <p>How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config for more information. Defaults to 100.</p> <code>100</code> <code>timeout</code> <code>timedelta | str</code> <p>Timeout value for the client. If a string is provided check https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.</p> <code>''</code> <code>**kwargs</code> <p>Unused kwargs for backwards compatibility with kubernetes library</p> <code>{}</code> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>def __init__(\n    self,\n    config: bytes | BytesIO | None = None,\n    qps: float = -1,\n    burst: int = 100,\n    timeout: datetime.timedelta | str = \"\",\n    **kwargs,\n):\n    \"\"\"Initialize the dynamic client with optional overrides\n\n    Args:\n        config (bytes | BytesIO | None, optional): The raw kube_config data to initialize the client with. Defaults\n            to controller-runtime's GetConfig.\n            See https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/client/config#GetConfig for more information.\n        qps (float, optional): QPS to set for the client. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n            for more information. Defaults to -1 which means rate-limiting is disabled.\n        burst (int, optional): How many requests can the client burst to. See https://pkg.go.dev/k8s.io/client-go/rest#Config\n            for more information. Defaults to 100.\n        timeout (datetime.timedelta | str, optional): Timeout value for the client. If a string is provided check\n            https://pkg.go.dev/time#ParseDuration for parsing information. Defaults to \"\" or no timeout.\n        **kwargs: Unused kwargs for backwards compatibility with kubernetes library\n    \"\"\"\n    # Parse timeout into a string\n    if isinstance(timeout, datetime.timedelta):\n        timeout = f\"{timeout.total_seconds()}s\"\n\n    # Setup the common client arguments\n    client_args = [qps, burst, timeout]\n    if config:\n        self.client = NewWrappedDynamicClientWithConfig(serialize_to_go(config), *client_args)\n    else:\n        self.client = NewWrappedDynamicClient(*client_args)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.client.DynamicClient.resource","title":"<code>resource(api_version, kind)</code>","text":"<p>Return a DynamicResource for the given api_version and kind. This DynamicResource can then be used to interact with the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>api_version</code> <code>str</code> <p>The api_version for the resource handle</p> required <code>kind</code> <code>str</code> <p>The kind for the resource handle</p> required <p>Returns:</p> Name Type Description <code>DynamicResource</code> <code>DynamicResource</code> <p>The DynamicResource handle for the requested type</p> Source code in <code>kubernetes_lite/client/client.py</code> <pre><code>def resource(self, api_version: str, kind: str) -&gt; DynamicResource:\n    \"\"\"Return a DynamicResource for the given api_version and kind. This\n    DynamicResource can then be used to interact with the cluster.\n\n    Args:\n        api_version (str): The api_version for the resource handle\n        kind (str): The kind for the resource handle\n\n    Returns:\n        DynamicResource: The DynamicResource handle for the requested type\n    \"\"\"\n    return self.resources.get(api_version, kind)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.discovery","title":"<code>discovery</code>","text":"<p>This module contains the objects used in discovering GVR's in the cluster</p>"},{"location":"api_references/#kubernetes_lite.client.discovery.Discoverer","title":"<code>Discoverer</code>","text":"<p>Discoverer is used to discover resource kinds/apiVersions and generate the corresponding DynamicResource</p> Source code in <code>kubernetes_lite/client/discovery.py</code> <pre><code>class Discoverer:\n    \"\"\"Discoverer is used to discover resource kinds/apiVersions and generate the\n    corresponding DynamicResource\n    \"\"\"\n\n    def __init__(self, client: WrappedDynamicClient):\n        \"\"\"Initialize the Discoverer with a provided client\n\n        Args:\n            client (WrappedDynamicClient): The client to use for interacting with the cluster\n        \"\"\"\n        self._client = client\n\n    @wrap_kube_error\n    def get(self, api_version: str, kind: str) -&gt; DynamicResource:\n        \"\"\"Return a DynamicResource for the given api_version and kind. This\n        DynamicResource can then be used to interact with the cluster.\n\n        Args:\n            api_version (str): The api_version for the resource handle\n            kind (str): The kind for the resource handle\n\n        Returns:\n            DynamicResource: The DynamicResource handle for the requested type\n        \"\"\"\n        kind = kind.lower()\n        return DynamicResource(self._client.Resource(api_version, kind))\n\n    def search(self, **kwargs) -&gt; DynamicResource:\n        \"\"\"See get for arguments/returns\"\"\"\n        warnings.warn(\n            \"The search method on resources/Discoverer is deprecated. Use the get method instead\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        return self.get(**kwargs)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.discovery.Discoverer.__init__","title":"<code>__init__(client)</code>","text":"<p>Initialize the Discoverer with a provided client</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>WrappedDynamicClient</code> <p>The client to use for interacting with the cluster</p> required Source code in <code>kubernetes_lite/client/discovery.py</code> <pre><code>def __init__(self, client: WrappedDynamicClient):\n    \"\"\"Initialize the Discoverer with a provided client\n\n    Args:\n        client (WrappedDynamicClient): The client to use for interacting with the cluster\n    \"\"\"\n    self._client = client\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.discovery.Discoverer.get","title":"<code>get(api_version, kind)</code>","text":"<p>Return a DynamicResource for the given api_version and kind. This DynamicResource can then be used to interact with the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>api_version</code> <code>str</code> <p>The api_version for the resource handle</p> required <code>kind</code> <code>str</code> <p>The kind for the resource handle</p> required <p>Returns:</p> Name Type Description <code>DynamicResource</code> <code>DynamicResource</code> <p>The DynamicResource handle for the requested type</p> Source code in <code>kubernetes_lite/client/discovery.py</code> <pre><code>@wrap_kube_error\ndef get(self, api_version: str, kind: str) -&gt; DynamicResource:\n    \"\"\"Return a DynamicResource for the given api_version and kind. This\n    DynamicResource can then be used to interact with the cluster.\n\n    Args:\n        api_version (str): The api_version for the resource handle\n        kind (str): The kind for the resource handle\n\n    Returns:\n        DynamicResource: The DynamicResource handle for the requested type\n    \"\"\"\n    kind = kind.lower()\n    return DynamicResource(self._client.Resource(api_version, kind))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.discovery.Discoverer.search","title":"<code>search(**kwargs)</code>","text":"<p>See get for arguments/returns</p> Source code in <code>kubernetes_lite/client/discovery.py</code> <pre><code>def search(self, **kwargs) -&gt; DynamicResource:\n    \"\"\"See get for arguments/returns\"\"\"\n    warnings.warn(\n        \"The search method on resources/Discoverer is deprecated. Use the get method instead\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    return self.get(**kwargs)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource","title":"<code>resource</code>","text":"<p>This module contains the DynamicResource which is the main way to interact with the cluster</p>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource","title":"<code>DynamicResource</code>","text":"<p>DynamicResource is the primary client for interacting with resources on the cluster. It's functions mirror the ResourceInterface provided by https://pkg.go.dev/k8s.io/client-go/dynamic#ResourceInterface. The parameters to the various functions map to the json names of the Options objects in https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1. For example <code>list()</code> parameter's map to ListOptions</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>class DynamicResource:\n    \"\"\"DynamicResource is the primary client for interacting with resources on the cluster. It's\n    functions mirror the ResourceInterface provided by https://pkg.go.dev/k8s.io/client-go/dynamic#ResourceInterface.\n    The parameters to the various functions map to the json names of the Options objects in\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1. For example `list()` parameter's map\n    to ListOptions\n    \"\"\"\n\n    def __init__(self, resource: WrappedNamespaceableResourceInterface):\n        \"\"\"Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class\n        should not be initialized directly and should instead be created by the client/discoverer\n\n        Args:\n            resource (WrappedNamespaceableResourceInterface): The WrappedNamespaceableResourceInterface to wrap\n        \"\"\"\n        self._resource = resource\n\n    @wrap_kube_error\n    def get(\n        self,\n        name: str | None = None,\n        namespace: str | None = None,\n        resource_version: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions\n\n        Args:\n            name (str | None, optional): The name of the resource to fetch. **None is Deprecated**. Defaults to None.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more GetOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The object from the cluster as a dictionary\n        \"\"\"\n        if name is None:\n            warnings.warn(\n                \"Get with no name to list objects is deprecated. Use the list method instead\",\n                DeprecationWarning,\n                stacklevel=2,\n            )\n            return self.list(namespace=namespace, resource_version=resource_version, **kwargs)\n\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Get(name, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def create(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The created object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Create(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def update(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The updated object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Update(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def update_status(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The updated object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.UpdateStatus(serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def patch(  # noqa: PLR0917, PLR0913\n        self,\n        name: str,\n        patch_type: str,\n        patch_data: SourceObjectType,\n        namespace: str | None = None,\n        dry_run: list[str] | None = None,\n        field_manager: str | None = None,\n        field_validation: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions\n\n        Args:\n            name (str): Name of the object to patch\n            patch_type (str): Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for\n                available options\n            patch_data (SourceObjectType): The raw patch data to use\n\n            resource (SourceObjectType): The resource to deploy\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more PatchOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The patched object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if field_validation:\n            options[\"fieldValidation\"] = field_validation\n        if field_manager:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_patch = serialize_to_go(patch_data)\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Patch(name, patch_type, serialized_patch, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def apply(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        name: str | None = None,\n        namespace: str | None = None,\n        dry_run: bool | None = None,\n        field_manager: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n        See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            name (str | None, optional): Optional name for the resource. Defaults to None or name\n                in the provided resources.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The applied object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if force:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        if not name and resource_metadata.name:\n            name = resource_metadata.name\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.Apply(name, serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def apply_status(  # noqa: PLR0917, PLR0913\n        self,\n        resource: SourceObjectType,\n        name: str | None = None,\n        namespace: str | None = None,\n        dry_run: bool | None = None,\n        field_manager: str | None = None,\n        force: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        subresources: list[str] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n        See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n        Args:\n            resource (SourceObjectType): The resource to deploy\n            name (str | None, optional): Optional name for the resource. Defaults to None or name\n                in the provided resources.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n                in the provided resources.\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n                Defaults to None.\n            force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n            field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n                handle objects in the request containing unknown or duplicate fields. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The applied object from the cluster in a dictionary\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if field_manager:\n            options[\"fieldManager\"] = field_manager\n        if force:\n            options[\"force\"] = force\n        serialized_options = serialize_to_go(options)\n\n        serialized_resource, resource_metadata = process_input_resource(resource)\n        if not namespace and resource_metadata.namespace:\n            namespace = resource_metadata.namespace\n        if not name and resource_metadata.name:\n            name = resource_metadata.name\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        subresource_options = subresources if subresources else []\n        result = resource_ptr.ApplyStatus(name, serialized_resource, serialized_options, *subresource_options)\n        return deserialize_from_go(result)\n\n    @wrap_kube_error\n    def delete(  # noqa: PLR0917, PLR0913\n        self,\n        name: str,\n        namespace: str | None = None,\n        grace_period_seconds: int | None = None,\n        propagation_policy: str | None = None,\n        dry_run: list[str] | None = None,\n        ignore_store_read_error: bool | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ):\n        \"\"\"Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.\n\n        Args:\n            name (str): Name for the resource to delete.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n                deleting the resource. Defaults to None.\n            propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n                to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n                for more information. Defaults to None.\n            ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n                if there is an etcd store error. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more DeleteOptions\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if grace_period_seconds:\n            options[\"gracePeriodSeconds\"] = grace_period_seconds\n        if propagation_policy:\n            options[\"PropagationPolicy\"] = propagation_policy\n        if dry_run:\n            options[\"dryRun\"] = dry_run\n        if ignore_store_read_error:\n            options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        resource_ptr.Delete(name, serialized_options)\n\n    @wrap_kube_error\n    def delete_collection(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        grace_period_seconds: int | None = None,\n        propagation_policy: str | None = None,\n        dry_run: list[str] | None = None,\n        limit: int | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        ignore_store_read_error: bool | None = None,\n        extra_delete_options_kwargs: dict[str, Any] | None = None,\n        extra_list_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ):\n        \"\"\"Delete a collection of objects from the cluster. The parameters map to both\n        https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and\n         https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n        Args:\n            name (str): Name for the resource to delete.\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n            dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n                from the golang docs. Defaults to None.\n            grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n                deleting the resource. Defaults to None.\n            propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n                to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n                for more information. Defaults to None.\n            ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n                if there is an etcd store error. Defaults to None.\n            extra_delete_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more\n                DeleteOptions parameters. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            extra_list_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n        \"\"\"\n        delete_options = {**(extra_delete_options_kwargs or {}), **kwargs}\n        if grace_period_seconds:\n            delete_options[\"gracePeriodSeconds\"] = grace_period_seconds\n        if propagation_policy:\n            delete_options[\"PropagationPolicy\"] = propagation_policy\n        if dry_run:\n            delete_options[\"dryRun\"] = dry_run\n        if ignore_store_read_error:\n            delete_options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n\n        list_options = {**(extra_list_options_kwargs or {}), **kwargs}\n        if resource_version:\n            list_options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            list_options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            list_options[\"labelSelector\"] = label_selector\n        if field_selector:\n            list_options[\"fieldSelector\"] = field_selector\n        if timeout:\n            list_options[\"timeoutSeconds\"] = timeout\n        if limit:\n            list_options[\"Limit\"] = limit\n        serialized_del_options = serialize_to_go(delete_options)\n        serialized_list_options = serialize_to_go(list_options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n        resource_ptr.DeleteCollection(serialized_del_options, serialized_list_options)\n\n    @wrap_kube_error\n    def watch(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        limit: int | None = None,\n        continue_req: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        watcher: Watch | None = None,\n        **kwargs,\n    ) -&gt; Watch:\n        \"\"\"Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions\n\n        Args:\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n                limit keywarg. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n            watcher (Watch | None, optional): Optional Watch object to use as the watcher interface. Users\n                can use this to stop the event stream during or outside of iteration\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n                resources\n        \"\"\"\n        options = {**(extra_options_kwargs or {}), **kwargs}\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            options[\"labelSelector\"] = label_selector\n        if field_selector:\n            options[\"fieldSelector\"] = field_selector\n        if timeout:\n            options[\"timeoutSeconds\"] = timeout\n        if limit:\n            options[\"Limit\"] = limit\n        if continue_req:\n            options[\"continue\"] = continue_req\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n\n        if not watcher:\n            watcher = Watch()\n\n        interface = resource_ptr.Watch(serialized_options)\n        watcher.interface = interface\n\n        return watcher\n\n    # ! Defined at bottom of class to avoid overriding built-in list\n    @wrap_kube_error\n    def list(  # noqa: PLR0917, PLR0913\n        self,\n        namespace: str | None = None,\n        limit: int | None = None,\n        resource_version: str | None = None,\n        resource_version_match: str | None = None,\n        label_selector: str | None = None,\n        field_selector: str | None = None,\n        timeout: int | None = None,\n        continue_req: str | None = None,\n        extra_options_kwargs: dict[str, Any] | None = None,\n        **kwargs,\n    ) -&gt; dict:\n        \"\"\"List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n        Args:\n            namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n            resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n                the ObjectList object. Defaults to None.\n            resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n                Defaults to None.\n            label_selector (str | None, optional): Optional label selector to filter returned resources.\n                Defaults to None.\n            field_selector (str | None, optional): Optional field selector to filter returned resources.\n                Defaults to None.\n            timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n            limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n            continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n                limit keywarg. Defaults to None.\n            extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n                parameters. Defaults to None.\n            subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n            **kwargs: Unused kwargs for compatibility\n\n        Returns:\n            dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n                resources\n        \"\"\"\n        options = {\n            **(extra_options_kwargs or {}),\n            **kwargs,\n        }\n        if resource_version:\n            options[\"resourceVersion\"] = resource_version\n        if resource_version_match:\n            options[\"resourceVersionMatch\"] = resource_version_match\n        if label_selector:\n            options[\"labelSelector\"] = label_selector\n        if field_selector:\n            options[\"fieldSelector\"] = field_selector\n        if timeout:\n            options[\"timeoutSeconds\"] = timeout\n        if limit:\n            options[\"Limit\"] = limit\n        if continue_req:\n            options[\"continue\"] = continue_req\n        serialized_options = serialize_to_go(options)\n\n        resource_ptr = self._resource\n        if namespace:\n            resource_ptr = resource_ptr.Namespace(namespace)\n        result = resource_ptr.List(serialized_options)\n        return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.__init__","title":"<code>__init__(resource)</code>","text":"<p>Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class should not be initialized directly and should instead be created by the client/discoverer</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>WrappedNamespaceableResourceInterface</code> <p>The WrappedNamespaceableResourceInterface to wrap</p> required Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>def __init__(self, resource: WrappedNamespaceableResourceInterface):\n    \"\"\"Create a DynamicResource which points to a WrappedNamespaceableResourceInterface from go. This class\n    should not be initialized directly and should instead be created by the client/discoverer\n\n    Args:\n        resource (WrappedNamespaceableResourceInterface): The WrappedNamespaceableResourceInterface to wrap\n    \"\"\"\n    self._resource = resource\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.apply","title":"<code>apply(resource, name=None, namespace=None, dry_run=None, field_manager=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions. See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>name</code> <code>str | None</code> <p>Optional name for the resource. Defaults to None or name in the provided resources.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ApplyOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The applied object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef apply(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    name: str | None = None,\n    namespace: str | None = None,\n    dry_run: bool | None = None,\n    field_manager: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Apply an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n    See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        name (str | None, optional): Optional name for the resource. Defaults to None or name\n            in the provided resources.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The applied object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if force:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    if not name and resource_metadata.name:\n        name = resource_metadata.name\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Apply(name, serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.apply_status","title":"<code>apply_status(resource, name=None, namespace=None, dry_run=None, field_manager=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions. See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>name</code> <code>str | None</code> <p>Optional name for the resource. Defaults to None or name in the provided resources.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> required <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ApplyOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The applied object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef apply_status(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    name: str | None = None,\n    namespace: str | None = None,\n    dry_run: bool | None = None,\n    field_manager: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Apply a status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ApplyOptions.\n    See https://kubernetes.io/docs/reference/using-api/server-side-apply/ for more information\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        name (str | None, optional): Optional name for the resource. Defaults to None or name\n            in the provided resources.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ApplyOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The applied object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if force:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    if not name and resource_metadata.name:\n        name = resource_metadata.name\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.ApplyStatus(name, serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.create","title":"<code>create(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The created object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef create(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Create an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#CreateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The created object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Create(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.delete","title":"<code>delete(name, namespace=None, grace_period_seconds=None, propagation_policy=None, dry_run=None, ignore_store_read_error=None, extra_options_kwargs=None, **kwargs)</code>","text":"<p>Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the resource to delete.</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>grace_period_seconds</code> <code>int | None</code> <p>Optional time to wait for graceful termination before deleting the resource. Defaults to None.</p> <code>None</code> <code>propagation_policy</code> <code>str | None</code> <p>Optional DeletionPropagation decides how a deletion propagates to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation for more information. Defaults to None.</p> <code>None</code> <code>ignore_store_read_error</code> <code>bool | None</code> <p>Optional override to force successful deletion even if there is an etcd store error. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more DeleteOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> required <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef delete(  # noqa: PLR0917, PLR0913\n    self,\n    name: str,\n    namespace: str | None = None,\n    grace_period_seconds: int | None = None,\n    propagation_policy: str | None = None,\n    dry_run: list[str] | None = None,\n    ignore_store_read_error: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n):\n    \"\"\"Delete an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions.\n\n    Args:\n        name (str): Name for the resource to delete.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n            deleting the resource. Defaults to None.\n        propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n            to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n            for more information. Defaults to None.\n        ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n            if there is an etcd store error. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more DeleteOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if grace_period_seconds:\n        options[\"gracePeriodSeconds\"] = grace_period_seconds\n    if propagation_policy:\n        options[\"PropagationPolicy\"] = propagation_policy\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if ignore_store_read_error:\n        options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    resource_ptr.Delete(name, serialized_options)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.delete_collection","title":"<code>delete_collection(namespace=None, grace_period_seconds=None, propagation_policy=None, dry_run=None, limit=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, ignore_store_read_error=None, extra_delete_options_kwargs=None, extra_list_options_kwargs=None, **kwargs)</code>","text":"<p>Delete a collection of objects from the cluster. The parameters map to both https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and  https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for the resource to delete.</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>grace_period_seconds</code> <code>int | None</code> <p>Optional time to wait for graceful termination before deleting the resource. Defaults to None.</p> <code>None</code> <code>propagation_policy</code> <code>str | None</code> <p>Optional DeletionPropagation decides how a deletion propagates to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation for more information. Defaults to None.</p> <code>None</code> <code>ignore_store_read_error</code> <code>bool | None</code> <p>Optional override to force successful deletion even if there is an etcd store error. Defaults to None.</p> <code>None</code> <code>extra_delete_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more DeleteOptions parameters. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>extra_list_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef delete_collection(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    grace_period_seconds: int | None = None,\n    propagation_policy: str | None = None,\n    dry_run: list[str] | None = None,\n    limit: int | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    ignore_store_read_error: bool | None = None,\n    extra_delete_options_kwargs: dict[str, Any] | None = None,\n    extra_list_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n):\n    \"\"\"Delete a collection of objects from the cluster. The parameters map to both\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeleteOptions and\n     https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n    Args:\n        name (str): Name for the resource to delete.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        grace_period_seconds (int | None, optional): Optional time to wait for graceful termination before\n            deleting the resource. Defaults to None.\n        propagation_policy (str | None, optional): Optional DeletionPropagation decides how a deletion propagates\n            to dependents of the object. See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#DeletionPropagation\n            for more information. Defaults to None.\n        ignore_store_read_error (bool | None, optional): Optional override to force successful deletion even\n            if there is an etcd store error. Defaults to None.\n        extra_delete_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more\n            DeleteOptions parameters. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        extra_list_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n    \"\"\"\n    delete_options = {**(extra_delete_options_kwargs or {}), **kwargs}\n    if grace_period_seconds:\n        delete_options[\"gracePeriodSeconds\"] = grace_period_seconds\n    if propagation_policy:\n        delete_options[\"PropagationPolicy\"] = propagation_policy\n    if dry_run:\n        delete_options[\"dryRun\"] = dry_run\n    if ignore_store_read_error:\n        delete_options[\"IgnoreStoreReadErrorWithClusterBreakingPotential\"] = ignore_store_read_error\n\n    list_options = {**(extra_list_options_kwargs or {}), **kwargs}\n    if resource_version:\n        list_options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        list_options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        list_options[\"labelSelector\"] = label_selector\n    if field_selector:\n        list_options[\"fieldSelector\"] = field_selector\n    if timeout:\n        list_options[\"timeoutSeconds\"] = timeout\n    if limit:\n        list_options[\"Limit\"] = limit\n    serialized_del_options = serialize_to_go(delete_options)\n    serialized_list_options = serialize_to_go(list_options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n    resource_ptr.DeleteCollection(serialized_del_options, serialized_list_options)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.get","title":"<code>get(name=None, namespace=None, resource_version=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>The name of the resource to fetch. None is Deprecated. Defaults to None.</p> <code>None</code> <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more GetOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The object from the cluster as a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef get(\n    self,\n    name: str | None = None,\n    namespace: str | None = None,\n    resource_version: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Get an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#GetOptions\n\n    Args:\n        name (str | None, optional): The name of the resource to fetch. **None is Deprecated**. Defaults to None.\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more GetOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The object from the cluster as a dictionary\n    \"\"\"\n    if name is None:\n        warnings.warn(\n            \"Get with no name to list objects is deprecated. Use the list method instead\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        return self.list(namespace=namespace, resource_version=resource_version, **kwargs)\n\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Get(name, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.list","title":"<code>list(namespace=None, limit=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, continue_req=None, extra_options_kwargs=None, **kwargs)</code>","text":"<p>List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>continue_req</code> <code>str | None</code> <p>Optional continue token to use to iterate over more results. Used with limit keywarg. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Optional subresource of an object to get. Defaults to None.</p> required <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The ObjectList object from the cluster as a dictionary e.g. use <code>.get(\"items\")</code> to get list of resources</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef list(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    limit: int | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    continue_req: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"List an object from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#ListOptions\n\n    Args:\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n            limit keywarg. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n            resources\n    \"\"\"\n    options = {\n        **(extra_options_kwargs or {}),\n        **kwargs,\n    }\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        options[\"labelSelector\"] = label_selector\n    if field_selector:\n        options[\"fieldSelector\"] = field_selector\n    if timeout:\n        options[\"timeoutSeconds\"] = timeout\n    if limit:\n        options[\"Limit\"] = limit\n    if continue_req:\n        options[\"continue\"] = continue_req\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n    result = resource_ptr.List(serialized_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.patch","title":"<code>patch(name, patch_type, patch_data, namespace=None, dry_run=None, field_manager=None, field_validation=None, force=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the object to patch</p> required <code>patch_type</code> <code>str</code> <p>Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for available options</p> required <code>patch_data</code> <code>SourceObjectType</code> <p>The raw patch data to use</p> required <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>force</code> <code>bool | None</code> <p>If the patch should be forced ignoring field_manager. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more PatchOptions parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The patched object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef patch(  # noqa: PLR0917, PLR0913\n    self,\n    name: str,\n    patch_type: str,\n    patch_data: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    force: bool | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Patch an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#PatchOptions\n\n    Args:\n        name (str): Name of the object to patch\n        patch_type (str): Type of patch to use. See https://pkg.go.dev/k8s.io/apimachinery/pkg/types#PatchType for\n            available options\n        patch_data (SourceObjectType): The raw patch data to use\n\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        force (bool | None, optional): If the patch should be forced ignoring field_manager. Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more PatchOptions\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The patched object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    if field_manager:\n        options[\"force\"] = force\n    serialized_options = serialize_to_go(options)\n\n    serialized_patch = serialize_to_go(patch_data)\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Patch(name, patch_type, serialized_patch, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.update","title":"<code>update(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The updated object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef update(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Update an object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The updated object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.Update(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.update_status","title":"<code>update_status(resource, namespace=None, dry_run=None, field_manager=None, field_validation=None, extra_options_kwargs=None, subresources=None, **kwargs)</code>","text":"<p>Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions</p> <p>Parameters:</p> Name Type Description Default <code>resource</code> <code>SourceObjectType</code> <p>The resource to deploy</p> required <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None or namespace in the provided resources.</p> <code>None</code> <code>dry_run</code> <code>list[str] | None</code> <p>Optional List of string dryrruns to use. Valid values can be gathered from the golang docs. Defaults to None.</p> <code>None</code> <code>field_manager</code> <code>str | None</code> <p>Optional field manager string to use to manage resource fields. Defaults to None.</p> <code>None</code> <code>field_validation</code> <code>str | None</code> <p>Optional fieldValidation instructs the server on how to handle objects in the request containing unknown or duplicate fields. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more CreateOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Subresource of an object to get. Defaults to None.</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The updated object from the cluster in a dictionary</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef update_status(  # noqa: PLR0917, PLR0913\n    self,\n    resource: SourceObjectType,\n    namespace: str | None = None,\n    dry_run: list[str] | None = None,\n    field_manager: str | None = None,\n    field_validation: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    subresources: list[str] | None = None,\n    **kwargs,\n) -&gt; dict:\n    \"\"\"Update the status object in the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#UpdateOptions\n\n    Args:\n        resource (SourceObjectType): The resource to deploy\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None or namespace\n            in the provided resources.\n        dry_run (list[str] | None, optional): Optional List of string dryrruns to use. Valid values can be gathered\n            from the golang docs. Defaults to None.\n        field_manager (str | None, optional): Optional field manager string to use to manage resource fields.\n            Defaults to None.\n        field_validation (str | None, optional): Optional fieldValidation instructs the server on how to\n            handle objects in the request containing unknown or duplicate fields. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more CreateOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Subresource of an object to get. Defaults to None.\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The updated object from the cluster in a dictionary\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if dry_run:\n        options[\"dryRun\"] = dry_run\n    if field_manager:\n        options[\"fieldManager\"] = field_manager\n    if field_validation:\n        options[\"fieldValidation\"] = field_validation\n    serialized_options = serialize_to_go(options)\n\n    serialized_resource, resource_metadata = process_input_resource(resource)\n    if not namespace and resource_metadata.namespace:\n        namespace = resource_metadata.namespace\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    subresource_options = subresources if subresources else []\n    result = resource_ptr.UpdateStatus(serialized_resource, serialized_options, *subresource_options)\n    return deserialize_from_go(result)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.resource.DynamicResource.watch","title":"<code>watch(namespace=None, resource_version=None, resource_version_match=None, label_selector=None, field_selector=None, timeout=None, limit=None, continue_req=None, extra_options_kwargs=None, watcher=None, **kwargs)</code>","text":"<p>Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str | None</code> <p>Optional namespace for the resource. Defaults to None.</p> <code>None</code> <code>resource_version</code> <code>str | None</code> <p>Optional resourceVersion to fetch. Maps to the resourceVersion of the ObjectList object. Defaults to None.</p> <code>None</code> <code>resource_version_match</code> <code>str | None</code> <p>Optional regex string for filtering returned resources. Defaults to None.</p> <code>None</code> <code>label_selector</code> <code>str | None</code> <p>Optional label selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>field_selector</code> <code>str | None</code> <p>Optional field selector to filter returned resources. Defaults to None.</p> <code>None</code> <code>timeout</code> <code>int | None</code> <p>Optional server timeout for the request. Defaults to None.</p> <code>None</code> <code>limit</code> <code>int | None</code> <p>Optional max number of resources returned. Defaults to None.</p> <code>None</code> <code>continue_req</code> <code>str | None</code> <p>Optional continue token to use to iterate over more results. Used with limit keywarg. Defaults to None.</p> <code>None</code> <code>extra_options_kwargs</code> <code>dict[str, Any] | None</code> <p>Optional dictionary containing more ListOption parameters. Defaults to None.</p> <code>None</code> <code>subresources</code> <code>list[str] | None</code> <p>Optional subresource of an object to get. Defaults to None.</p> required <code>watcher</code> <code>Watch | None</code> <p>Optional Watch object to use as the watcher interface. Users can use this to stop the event stream during or outside of iteration</p> <code>None</code> <code>**kwargs</code> <p>Unused kwargs for compatibility</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>Watch</code> <p>The ObjectList object from the cluster as a dictionary e.g. use <code>.get(\"items\")</code> to get list of resources</p> Source code in <code>kubernetes_lite/client/resource.py</code> <pre><code>@wrap_kube_error\ndef watch(  # noqa: PLR0917, PLR0913\n    self,\n    namespace: str | None = None,\n    resource_version: str | None = None,\n    resource_version_match: str | None = None,\n    label_selector: str | None = None,\n    field_selector: str | None = None,\n    timeout: int | None = None,\n    limit: int | None = None,\n    continue_req: str | None = None,\n    extra_options_kwargs: dict[str, Any] | None = None,\n    watcher: Watch | None = None,\n    **kwargs,\n) -&gt; Watch:\n    \"\"\"Watch for events from the cluster. The parameters map to https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#WatchOptions\n\n    Args:\n        namespace (str | None, optional): Optional namespace for the resource. Defaults to None.\n        resource_version (str | None, optional): Optional resourceVersion to fetch. Maps to the resourceVersion of\n            the ObjectList object. Defaults to None.\n        resource_version_match (str | None, optional): Optional regex string for filtering returned resources.\n            Defaults to None.\n        label_selector (str | None, optional): Optional label selector to filter returned resources.\n            Defaults to None.\n        field_selector (str | None, optional): Optional field selector to filter returned resources.\n            Defaults to None.\n        timeout (int | None, optional): Optional server timeout for the request. Defaults to None.\n        limit (int | None, optional): Optional max number of resources returned. Defaults to None.\n        continue_req (str | None, optional): Optional continue token to use to iterate over more results. Used with\n            limit keywarg. Defaults to None.\n        extra_options_kwargs (dict[str, Any] | None, optional): Optional dictionary containing more ListOption\n            parameters. Defaults to None.\n        subresources (list[str] | None, optional): Optional subresource of an object to get. Defaults to None.\n        watcher (Watch | None, optional): Optional Watch object to use as the watcher interface. Users\n            can use this to stop the event stream during or outside of iteration\n        **kwargs: Unused kwargs for compatibility\n\n    Returns:\n        dict: The ObjectList object from the cluster as a dictionary e.g. use `.get(\"items\")` to get list of\n            resources\n    \"\"\"\n    options = {**(extra_options_kwargs or {}), **kwargs}\n    if resource_version:\n        options[\"resourceVersion\"] = resource_version\n    if resource_version_match:\n        options[\"resourceVersionMatch\"] = resource_version_match\n    if label_selector:\n        options[\"labelSelector\"] = label_selector\n    if field_selector:\n        options[\"fieldSelector\"] = field_selector\n    if timeout:\n        options[\"timeoutSeconds\"] = timeout\n    if limit:\n        options[\"Limit\"] = limit\n    if continue_req:\n        options[\"continue\"] = continue_req\n    serialized_options = serialize_to_go(options)\n\n    resource_ptr = self._resource\n    if namespace:\n        resource_ptr = resource_ptr.Namespace(namespace)\n\n    if not watcher:\n        watcher = Watch()\n\n    interface = resource_ptr.Watch(serialized_options)\n    watcher.interface = interface\n\n    return watcher\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.sanitization","title":"<code>sanitization</code>","text":"<p>This module contains code around sanitizing an object for deployment</p>"},{"location":"api_references/#kubernetes_lite.client.sanitization.ObjectMetadata","title":"<code>ObjectMetadata</code>  <code>dataclass</code>","text":"<p>ObjectMetadata is used to describe extracted information from an object</p> <p>Attributes:</p> Name Type Description <code>metadata</code> <code>dict[str, Any]</code> <p>Metadata extracted from the object</p> <code>name</code> <code>str</code> <p>Name of the object</p> <code>namespace</code> <code>str | None</code> <p>Optional namespace of the object</p> Source code in <code>kubernetes_lite/client/sanitization.py</code> <pre><code>@dataclass\nclass ObjectMetadata:\n    \"\"\"ObjectMetadata is used to describe extracted information from an object\n\n    Attributes:\n        metadata (dict[str, Any]): Metadata extracted from the object\n        name (str):  Name of the object\n        namespace (str | None): Optional namespace of the object\n\n    \"\"\"\n\n    metadata: dict[str, Any]\n    name: str\n    namespace: str | None\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.sanitization.process_input_resource","title":"<code>process_input_resource(obj)</code>","text":"<p>Sanitize and serialize an object into go. After sanitizing extract some metadata about the object.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>SanitizedObjectType</code> <p>The object to sanitize/serialize</p> required <p>Returns:</p> Type Description <code>tuple[Slice_byte, ObjectMetadata]</code> <p>tuple[Slice_byte, ObjectMetadata]: The serialized go object and extracted metadata</p> Source code in <code>kubernetes_lite/client/sanitization.py</code> <pre><code>def process_input_resource(obj: SanitizedObjectType) -&gt; tuple[Slice_byte, ObjectMetadata]:\n    \"\"\"Sanitize and serialize an object into go. After sanitizing extract some metadata about the\n    object.\n\n    Args:\n        obj (SanitizedObjectType): The object to sanitize/serialize\n\n    Returns:\n        tuple[Slice_byte, ObjectMetadata]: The serialized go object and extracted metadata\n    \"\"\"\n    sanitized_obj = sanitize_resource_for_serialization(obj)\n\n    if not isinstance(sanitized_obj, dict):\n        raise RuntimeError(f\"can not deploy object of type {type(sanitized_obj)}\")\n\n    # Extract metadata from sanitized object\n    metadata = ObjectMetadata(\n        name=sanitized_obj.get(\"metadata\", {}).get(\"name\"),\n        namespace=sanitized_obj.get(\"metadata\", {}).get(\"namespace\"),\n        metadata=sanitized_obj.get(\"metadata\", {}),\n    )\n\n    return (serialize_to_go(sanitized_obj), metadata)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.sanitization.sanitize_resource_for_serialization","title":"<code>sanitize_resource_for_serialization(obj)</code>","text":"<p>Sanitize an object before serializing it to bytes If obj is None, return None. If obj is str, int, long, float, bool, return directly. If obj is datetime.datetime, datetime.date     convert to string in iso8601 format. If obj is list, sanitize each element in the list. If obj is dict, return the dict. If obj is OpenAPI model, return the properties dict. :param obj: The data to serialize. :return: The serialized form of data.</p> Source code in <code>kubernetes_lite/client/sanitization.py</code> <pre><code>def sanitize_resource_for_serialization(obj: SourceObjectType) -&gt; SanitizedObjectType:  # noqa: PLR0911, PLR0912\n    \"\"\"Sanitize an object before serializing it to bytes\n    If obj is None, return None.\n    If obj is str, int, long, float, bool, return directly.\n    If obj is datetime.datetime, datetime.date\n        convert to string in iso8601 format.\n    If obj is list, sanitize each element in the list.\n    If obj is dict, return the dict.\n    If obj is OpenAPI model, return the properties dict.\n    :param obj: The data to serialize.\n    :return: The serialized form of data.\n    \"\"\"\n    if obj is None:  # pylint: disable=no-else-return\n        return None\n    elif isinstance(obj, float | bool | bytes | str | int):\n        return obj\n    elif isinstance(obj, list):\n        return [sanitize_resource_for_serialization(sub_obj) for sub_obj in obj]\n    elif isinstance(obj, tuple):\n        return tuple(sanitize_resource_for_serialization(sub_obj) for sub_obj in obj)\n    elif isinstance(obj, datetime.datetime | datetime.date):\n        return obj.isoformat()\n    elif isinstance(obj, property):\n        return sanitize_resource_for_serialization(obj.fget())\n\n    if isinstance(obj, dict):\n        obj_dict = obj\n    elif hasattr(obj, \"attribute_map\"):\n        # Convert model obj to dict except\n        # `openapi_types` and `attribute_map`.\n        # Convert attribute name to json key in\n        # model definition for request.\n        obj_dict = {}\n        for attr, name in obj.attribute_map.items():\n            if hasattr(obj, attr):\n                obj_dict[name] = getattr(obj, attr)\n    else:\n        # Hope that the object can still be serialized by the json encoder.\n        return obj\n\n    # Prune fields which are None but keep\n    # empty arrays or dictionaries\n    return_dict = {}\n    for key, val in obj_dict.items():\n        updated_obj = sanitize_resource_for_serialization(val)\n        if updated_obj is not None:\n            return_dict[key] = updated_obj\n    return return_dict\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.watch","title":"<code>watch</code>","text":"<p>This module contains the interface for iterating over events from the cluster</p>"},{"location":"api_references/#kubernetes_lite.client.watch.Watch","title":"<code>Watch</code>","text":"<p>Watch is an iterator that yields events streamed from the cluster</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>class Watch:\n    \"\"\"Watch is an iterator that yields events streamed from\n    the cluster\n    \"\"\"\n\n    interface: WrappedWatchInterface | None = None\n\n    def __init__(self, interface: WrappedWatchInterface | None = None):\n        \"\"\"Initialize the Watch instance with a given wrapped interface\n\n        Args:\n            interface (WrappedWatchInterface | None, optional): The interface to yield events from. Defaults to None.\n        \"\"\"\n        self.interface = interface\n\n    def stop(self):\n        \"\"\"Stop streaming events from the cluster\"\"\"\n        if not self.interface:\n            raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n        self.interface.Stop()\n\n    def __iter__(self) -&gt; \"Watch\":\n        \"\"\"Complete implementation of a python iterator\n\n        Returns:\n            Watch: Itself to iterate\n        \"\"\"\n        return self\n\n    def __next__(self) -&gt; dict:\n        \"\"\"Return the next event from the cluster. These events are dictionaries in the\n        format of json encoded apimachinery Events. You can find more information here:\n        https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event\n\n        Returns:\n            dict: The event data\n        \"\"\"\n        if not self.interface:\n            raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n        # Try to get an event from the underlying interface\n        try:\n            event_data = self.interface.Next()\n        except RuntimeError as exc:\n            if \"stop iteration\" in str(exc):\n                raise StopIteration from None\n\n        # Return the deserialized result\n        return deserialize_result(bytes(event_data))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.watch.Watch.__init__","title":"<code>__init__(interface=None)</code>","text":"<p>Initialize the Watch instance with a given wrapped interface</p> <p>Parameters:</p> Name Type Description Default <code>interface</code> <code>WrappedWatchInterface | None</code> <p>The interface to yield events from. Defaults to None.</p> <code>None</code> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __init__(self, interface: WrappedWatchInterface | None = None):\n    \"\"\"Initialize the Watch instance with a given wrapped interface\n\n    Args:\n        interface (WrappedWatchInterface | None, optional): The interface to yield events from. Defaults to None.\n    \"\"\"\n    self.interface = interface\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.watch.Watch.__iter__","title":"<code>__iter__()</code>","text":"<p>Complete implementation of a python iterator</p> <p>Returns:</p> Name Type Description <code>Watch</code> <code>Watch</code> <p>Itself to iterate</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __iter__(self) -&gt; \"Watch\":\n    \"\"\"Complete implementation of a python iterator\n\n    Returns:\n        Watch: Itself to iterate\n    \"\"\"\n    return self\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.watch.Watch.__next__","title":"<code>__next__()</code>","text":"<p>Return the next event from the cluster. These events are dictionaries in the format of json encoded apimachinery Events. You can find more information here: https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The event data</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def __next__(self) -&gt; dict:\n    \"\"\"Return the next event from the cluster. These events are dictionaries in the\n    format of json encoded apimachinery Events. You can find more information here:\n    https://pkg.go.dev/k8s.io/apimachinery/pkg/watch#Event\n\n    Returns:\n        dict: The event data\n    \"\"\"\n    if not self.interface:\n        raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n    # Try to get an event from the underlying interface\n    try:\n        event_data = self.interface.Next()\n    except RuntimeError as exc:\n        if \"stop iteration\" in str(exc):\n            raise StopIteration from None\n\n    # Return the deserialized result\n    return deserialize_result(bytes(event_data))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.client.watch.Watch.stop","title":"<code>stop()</code>","text":"<p>Stop streaming events from the cluster</p> Source code in <code>kubernetes_lite/client/watch.py</code> <pre><code>def stop(self):\n    \"\"\"Stop streaming events from the cluster\"\"\"\n    if not self.interface:\n        raise RuntimeError(\"Interface must be set before calling stop/next\")\n\n    self.interface.Stop()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest","title":"<code>envtest</code>","text":"<p>This module contains a pythonic wrapper around the https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest EnvTest object</p>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest","title":"<code>EnvTest</code>","text":"<p>EnvTest is a wrapper around the golang EnvTest object provided by https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest. This class exposes methods to start and stop the server as well as fetch a raw kubeconfig or client object. This class can also handle calling setup-envtest to download the required binaries and create the path.</p> <p>To configure the underyling golang EnvTestEnvironment checkout the environmental variables exposed by EnvTest: https://book.kubebuilder.io/reference/envtest.html#environment-variables</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>class EnvTest:\n    \"\"\"EnvTest is a wrapper around the golang EnvTest object provided by\n    https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest. This class\n    exposes methods to start and stop the server as well as fetch a raw kubeconfig\n    or client object. This class can also handle calling setup-envtest to download\n    the required binaries and create the path.\n\n    To configure the underyling golang EnvTestEnvironment checkout the environmental variables\n    exposed by EnvTest: https://book.kubebuilder.io/reference/envtest.html#environment-variables\n    \"\"\"\n\n    def __init__(self, log: Logger | None = None):\n        \"\"\"Initialize the EnvTest object and create an event to ensure the server isn't\n        shutdown multiple times\n\n        Arguments:\n            log (logger | None): Optional logger to provide. Defaults to None\n        \"\"\"\n        self._log = log if log else getLogger(\"envtest\")\n        self._env: EnvTestEnvironment | None = None\n        self._stop_event = Event()\n\n    @cached_property\n    def env(self) -&gt; EnvTestEnvironment:\n        \"\"\"Return the underlying golang EnvTestEnvironment object. Must be called\n        after starting the server\n\n        Returns:\n            EnvTestEnvironment: The underlying golang object for this instance\n        \"\"\"\n        if not self._env:\n            raise RuntimeError(\"Must start environment before accessing it\")\n        return self._env\n\n    def start(self, auto_setup: bool = True) -&gt; BytesIO:\n        \"\"\"Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test\n        to download the correct kube bins.\n\n        Args:\n            auto_setup (bool, optional): If setup-envtest should be ran to fetch the local\n                path. Defaults to True.\n\n        Returns:\n            BytesIO: A raw kube_config for interacting with the cluster.\n        \"\"\"\n        path = None\n        if auto_setup:\n            result = internal_run_setup_envtest_command(\"use\", \"-p\", \"path\")\n            result.log_and_raise(self._log, raise_exc=True)\n            path = result.stdout\n\n        if path:\n            self._env = NewEnvTestEnvironmentWithPath(path)\n        else:\n            self._env = NewEnvTestEnvironment()\n\n        self.env.Start()\n        atexit.register(self.stop)\n\n        return self.config()\n\n    def stop(self):\n        \"\"\"Stop the envtest server if it hasn't already been stopped\"\"\"\n        if self._stop_event.is_set():\n            return\n\n        self.env.Stop()\n        self._stop_event.set()\n\n    def config(self) -&gt; BytesIO:\n        \"\"\"Fetch a kubeconfig object for the given EnvTest server. Must be called\n        after start()\n\n        Returns:\n            BytesIO: The raw byte stream of the kube object\n        \"\"\"\n        return BytesIO(bytes(self.env.GetKubeConfig()))\n\n    def client(self) -&gt; DynamicClient:\n        \"\"\"Fetch a dynamic client object for interacting with the EnvTest server. Must be called\n        after start()\n\n        Returns:\n            DynamicClient: A client to interact with the server\n        \"\"\"\n        return DynamicClient(self.config())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.env","title":"<code>env</code>  <code>cached</code> <code>property</code>","text":"<p>Return the underlying golang EnvTestEnvironment object. Must be called after starting the server</p> <p>Returns:</p> Name Type Description <code>EnvTestEnvironment</code> <code>EnvTestEnvironment</code> <p>The underlying golang object for this instance</p>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.__init__","title":"<code>__init__(log=None)</code>","text":"<p>Initialize the EnvTest object and create an event to ensure the server isn't shutdown multiple times</p> <p>Parameters:</p> Name Type Description Default <code>log</code> <code>logger | None</code> <p>Optional logger to provide. Defaults to None</p> <code>None</code> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def __init__(self, log: Logger | None = None):\n    \"\"\"Initialize the EnvTest object and create an event to ensure the server isn't\n    shutdown multiple times\n\n    Arguments:\n        log (logger | None): Optional logger to provide. Defaults to None\n    \"\"\"\n    self._log = log if log else getLogger(\"envtest\")\n    self._env: EnvTestEnvironment | None = None\n    self._stop_event = Event()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.client","title":"<code>client()</code>","text":"<p>Fetch a dynamic client object for interacting with the EnvTest server. Must be called after start()</p> <p>Returns:</p> Name Type Description <code>DynamicClient</code> <code>DynamicClient</code> <p>A client to interact with the server</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def client(self) -&gt; DynamicClient:\n    \"\"\"Fetch a dynamic client object for interacting with the EnvTest server. Must be called\n    after start()\n\n    Returns:\n        DynamicClient: A client to interact with the server\n    \"\"\"\n    return DynamicClient(self.config())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.config","title":"<code>config()</code>","text":"<p>Fetch a kubeconfig object for the given EnvTest server. Must be called after start()</p> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>The raw byte stream of the kube object</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def config(self) -&gt; BytesIO:\n    \"\"\"Fetch a kubeconfig object for the given EnvTest server. Must be called\n    after start()\n\n    Returns:\n        BytesIO: The raw byte stream of the kube object\n    \"\"\"\n    return BytesIO(bytes(self.env.GetKubeConfig()))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.start","title":"<code>start(auto_setup=True)</code>","text":"<p>Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test to download the correct kube bins.</p> <p>Parameters:</p> Name Type Description Default <code>auto_setup</code> <code>bool</code> <p>If setup-envtest should be ran to fetch the local path. Defaults to True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>A raw kube_config for interacting with the cluster.</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def start(self, auto_setup: bool = True) -&gt; BytesIO:\n    \"\"\"Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test\n    to download the correct kube bins.\n\n    Args:\n        auto_setup (bool, optional): If setup-envtest should be ran to fetch the local\n            path. Defaults to True.\n\n    Returns:\n        BytesIO: A raw kube_config for interacting with the cluster.\n    \"\"\"\n    path = None\n    if auto_setup:\n        result = internal_run_setup_envtest_command(\"use\", \"-p\", \"path\")\n        result.log_and_raise(self._log, raise_exc=True)\n        path = result.stdout\n\n    if path:\n        self._env = NewEnvTestEnvironmentWithPath(path)\n    else:\n        self._env = NewEnvTestEnvironment()\n\n    self.env.Start()\n    atexit.register(self.stop)\n\n    return self.config()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.EnvTest.stop","title":"<code>stop()</code>","text":"<p>Stop the envtest server if it hasn't already been stopped</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def stop(self):\n    \"\"\"Stop the envtest server if it hasn't already been stopped\"\"\"\n    if self._stop_event.is_set():\n        return\n\n    self.env.Stop()\n    self._stop_event.set()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest","title":"<code>envtest</code>","text":"<p>This module contains the implementation fo the pythonic wrapper around https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest</p>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest","title":"<code>EnvTest</code>","text":"<p>EnvTest is a wrapper around the golang EnvTest object provided by https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest. This class exposes methods to start and stop the server as well as fetch a raw kubeconfig or client object. This class can also handle calling setup-envtest to download the required binaries and create the path.</p> <p>To configure the underyling golang EnvTestEnvironment checkout the environmental variables exposed by EnvTest: https://book.kubebuilder.io/reference/envtest.html#environment-variables</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>class EnvTest:\n    \"\"\"EnvTest is a wrapper around the golang EnvTest object provided by\n    https://pkg.go.dev/sigs.k8s.io/controller-runtime/pkg/envtest. This class\n    exposes methods to start and stop the server as well as fetch a raw kubeconfig\n    or client object. This class can also handle calling setup-envtest to download\n    the required binaries and create the path.\n\n    To configure the underyling golang EnvTestEnvironment checkout the environmental variables\n    exposed by EnvTest: https://book.kubebuilder.io/reference/envtest.html#environment-variables\n    \"\"\"\n\n    def __init__(self, log: Logger | None = None):\n        \"\"\"Initialize the EnvTest object and create an event to ensure the server isn't\n        shutdown multiple times\n\n        Arguments:\n            log (logger | None): Optional logger to provide. Defaults to None\n        \"\"\"\n        self._log = log if log else getLogger(\"envtest\")\n        self._env: EnvTestEnvironment | None = None\n        self._stop_event = Event()\n\n    @cached_property\n    def env(self) -&gt; EnvTestEnvironment:\n        \"\"\"Return the underlying golang EnvTestEnvironment object. Must be called\n        after starting the server\n\n        Returns:\n            EnvTestEnvironment: The underlying golang object for this instance\n        \"\"\"\n        if not self._env:\n            raise RuntimeError(\"Must start environment before accessing it\")\n        return self._env\n\n    def start(self, auto_setup: bool = True) -&gt; BytesIO:\n        \"\"\"Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test\n        to download the correct kube bins.\n\n        Args:\n            auto_setup (bool, optional): If setup-envtest should be ran to fetch the local\n                path. Defaults to True.\n\n        Returns:\n            BytesIO: A raw kube_config for interacting with the cluster.\n        \"\"\"\n        path = None\n        if auto_setup:\n            result = internal_run_setup_envtest_command(\"use\", \"-p\", \"path\")\n            result.log_and_raise(self._log, raise_exc=True)\n            path = result.stdout\n\n        if path:\n            self._env = NewEnvTestEnvironmentWithPath(path)\n        else:\n            self._env = NewEnvTestEnvironment()\n\n        self.env.Start()\n        atexit.register(self.stop)\n\n        return self.config()\n\n    def stop(self):\n        \"\"\"Stop the envtest server if it hasn't already been stopped\"\"\"\n        if self._stop_event.is_set():\n            return\n\n        self.env.Stop()\n        self._stop_event.set()\n\n    def config(self) -&gt; BytesIO:\n        \"\"\"Fetch a kubeconfig object for the given EnvTest server. Must be called\n        after start()\n\n        Returns:\n            BytesIO: The raw byte stream of the kube object\n        \"\"\"\n        return BytesIO(bytes(self.env.GetKubeConfig()))\n\n    def client(self) -&gt; DynamicClient:\n        \"\"\"Fetch a dynamic client object for interacting with the EnvTest server. Must be called\n        after start()\n\n        Returns:\n            DynamicClient: A client to interact with the server\n        \"\"\"\n        return DynamicClient(self.config())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.env","title":"<code>env</code>  <code>cached</code> <code>property</code>","text":"<p>Return the underlying golang EnvTestEnvironment object. Must be called after starting the server</p> <p>Returns:</p> Name Type Description <code>EnvTestEnvironment</code> <code>EnvTestEnvironment</code> <p>The underlying golang object for this instance</p>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.__init__","title":"<code>__init__(log=None)</code>","text":"<p>Initialize the EnvTest object and create an event to ensure the server isn't shutdown multiple times</p> <p>Parameters:</p> Name Type Description Default <code>log</code> <code>logger | None</code> <p>Optional logger to provide. Defaults to None</p> <code>None</code> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def __init__(self, log: Logger | None = None):\n    \"\"\"Initialize the EnvTest object and create an event to ensure the server isn't\n    shutdown multiple times\n\n    Arguments:\n        log (logger | None): Optional logger to provide. Defaults to None\n    \"\"\"\n    self._log = log if log else getLogger(\"envtest\")\n    self._env: EnvTestEnvironment | None = None\n    self._stop_event = Event()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.client","title":"<code>client()</code>","text":"<p>Fetch a dynamic client object for interacting with the EnvTest server. Must be called after start()</p> <p>Returns:</p> Name Type Description <code>DynamicClient</code> <code>DynamicClient</code> <p>A client to interact with the server</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def client(self) -&gt; DynamicClient:\n    \"\"\"Fetch a dynamic client object for interacting with the EnvTest server. Must be called\n    after start()\n\n    Returns:\n        DynamicClient: A client to interact with the server\n    \"\"\"\n    return DynamicClient(self.config())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.config","title":"<code>config()</code>","text":"<p>Fetch a kubeconfig object for the given EnvTest server. Must be called after start()</p> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>The raw byte stream of the kube object</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def config(self) -&gt; BytesIO:\n    \"\"\"Fetch a kubeconfig object for the given EnvTest server. Must be called\n    after start()\n\n    Returns:\n        BytesIO: The raw byte stream of the kube object\n    \"\"\"\n    return BytesIO(bytes(self.env.GetKubeConfig()))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.start","title":"<code>start(auto_setup=True)</code>","text":"<p>Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test to download the correct kube bins.</p> <p>Parameters:</p> Name Type Description Default <code>auto_setup</code> <code>bool</code> <p>If setup-envtest should be ran to fetch the local path. Defaults to True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>A raw kube_config for interacting with the cluster.</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def start(self, auto_setup: bool = True) -&gt; BytesIO:\n    \"\"\"Construct and start the EnvTestEnvironment object. Can optionally run setupenv-test\n    to download the correct kube bins.\n\n    Args:\n        auto_setup (bool, optional): If setup-envtest should be ran to fetch the local\n            path. Defaults to True.\n\n    Returns:\n        BytesIO: A raw kube_config for interacting with the cluster.\n    \"\"\"\n    path = None\n    if auto_setup:\n        result = internal_run_setup_envtest_command(\"use\", \"-p\", \"path\")\n        result.log_and_raise(self._log, raise_exc=True)\n        path = result.stdout\n\n    if path:\n        self._env = NewEnvTestEnvironmentWithPath(path)\n    else:\n        self._env = NewEnvTestEnvironment()\n\n    self.env.Start()\n    atexit.register(self.stop)\n\n    return self.config()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.envtest.EnvTest.stop","title":"<code>stop()</code>","text":"<p>Stop the envtest server if it hasn't already been stopped</p> Source code in <code>kubernetes_lite/envtest/envtest.py</code> <pre><code>def stop(self):\n    \"\"\"Stop the envtest server if it hasn't already been stopped\"\"\"\n    if self._stop_event.is_set():\n        return\n\n    self.env.Stop()\n    self._stop_event.set()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest","title":"<code>pytest</code>","text":"<p>This module contains helper pytest fixtures to make using EnvTest easier in testing environments</p>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.create_random_namespace","title":"<code>create_random_namespace(client)</code>","text":"<p>Helper function to create a randomly named namespace from the given client</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>DynamicClient</code> <p>The client to create namespaces with</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The randomly generated namespace name</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>def create_random_namespace(client: DynamicClient) -&gt; str:\n    \"\"\"Helper function to create a randomly named namespace from the given client\n\n    Args:\n        client (DynamicClient): The client to create namespaces with\n\n    Returns:\n        str: The randomly generated namespace name\n    \"\"\"\n    # Randomize the namespace string\n    namespace = \"\".join(random.choice(string.ascii_lowercase) for i in range(10))\n    client.resources.get(api_version=\"v1\", kind=\"Namespace\").apply(\n        {\"metadata\": {\"name\": namespace}, \"kind\": \"Namespace\", \"apiVersion\": \"v1\"}, fieldManager=\"default\"\n    )\n    return namespace\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.dynamic_client","title":"<code>dynamic_client(kubernetes_env)</code>","text":"<p>A pytest fixture for a dynamic client connected to an EnvTest instance</p> <p>Parameters:</p> Name Type Description Default <code>kubernetes_env</code> <code>EnvTest</code> <p>a running EnvTest instance</p> required <p>Yields:</p> Type Description <code>DynamicClient</code> <p>Generator[DynamicClient, None, None]: The dynamic client that can be used to interact with an EnvTest instance</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture()\ndef dynamic_client(kubernetes_env: EnvTest) -&gt; Generator[DynamicClient, None, None]:\n    \"\"\"A pytest fixture for a dynamic client connected to an EnvTest instance\n\n    Args:\n        kubernetes_env (EnvTest): a running EnvTest instance\n\n    Yields:\n        Generator[DynamicClient, None, None]: The dynamic client that can be used to interact\n            with an EnvTest instance\n    \"\"\"\n    yield kubernetes_env.client()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.dynamic_client_namespace","title":"<code>dynamic_client_namespace(dynamic_client)</code>","text":"<p>Pytest fixture to return a Dynamic Client with a fresh namespace you can apply resources to</p> <p>Parameters:</p> Name Type Description Default <code>dynamic_client</code> <code>DynamicClient</code> <p>Dynamic Client fixture</p> required <p>Yields:</p> Type Description <code>tuple[DynamicClient, str]</code> <p>Generator[tuple[DynamicClient, str], None, None]: tuple with the client and namespace name</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture()\ndef dynamic_client_namespace(dynamic_client: DynamicClient) -&gt; Generator[tuple[DynamicClient, str], None, None]:\n    \"\"\"Pytest fixture to return a Dynamic Client with a fresh namespace you can apply resources to\n\n    Args:\n        dynamic_client (DynamicClient): Dynamic Client fixture\n\n    Yields:\n        Generator[tuple[DynamicClient, str], None, None]: tuple with the client and namespace name\n    \"\"\"\n    yield dynamic_client, create_random_namespace(dynamic_client)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.kubernetes_env","title":"<code>kubernetes_env()</code>","text":"<p>Pytest fixture for a running EnvTest instance</p> <p>Yields:</p> Type Description <code>EnvTest</code> <p>Generator[EnvTest, None, None]: The running EnvTest instance</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture()\ndef kubernetes_env() -&gt; Generator[EnvTest, None, None]:\n    \"\"\"Pytest fixture for a running EnvTest instance\n\n    Yields:\n        Generator[EnvTest, None, None]: The running EnvTest instance\n    \"\"\"\n    with yield_kubernetes_env() as env:\n        yield env\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.session_client","title":"<code>session_client(session_kubernetes_env)</code>","text":"<p>A session scoped pytest fixture for a dynamic client connected to an EnvTest instance</p> <p>Parameters:</p> Name Type Description Default <code>session_kubernetes_env</code> <code>EnvTest</code> <p>a running EnvTest instance</p> required <p>Yields:</p> Type Description <code>DynamicClient</code> <p>Generator[DynamicClient, None, None]: The dynamic client that can be used to interact with an EnvTest instance</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture(scope=\"session\")\ndef session_client(session_kubernetes_env: EnvTest) -&gt; Generator[DynamicClient, None, None]:\n    \"\"\"A session scoped pytest fixture for a dynamic client connected to an EnvTest\n    instance\n\n    Args:\n        session_kubernetes_env (EnvTest): a running EnvTest instance\n\n    Yields:\n        Generator[DynamicClient, None, None]: The dynamic client that can be used to interact\n            with an EnvTest instance\n    \"\"\"\n    yield session_kubernetes_env.client()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.session_client_namespace","title":"<code>session_client_namespace(session_client)</code>","text":"<p>Pytest session scoped fixture to return a Dynamic Client with a fresh namespace you can apply resources to</p> <p>Parameters:</p> Name Type Description Default <code>session_client</code> <code>DynamicClient</code> <p>Dynamic Client fixture</p> required <p>Yields:</p> Type Description <code>tuple[DynamicClient, str]</code> <p>Generator[tuple[DynamicClient, str], None, None]: tuple with the client and namespace name</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture()\ndef session_client_namespace(session_client: DynamicClient) -&gt; Generator[tuple[DynamicClient, str], None, None]:\n    \"\"\"Pytest session scoped fixture to return a Dynamic Client with a fresh namespace you can apply resources to\n\n    Args:\n        session_client (DynamicClient): Dynamic Client fixture\n\n    Yields:\n        Generator[tuple[DynamicClient, str], None, None]: tuple with the client and namespace name\n    \"\"\"\n    yield session_client, create_random_namespace(session_client)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.session_kubernetes_env","title":"<code>session_kubernetes_env()</code>","text":"<p>Session scoped version of the above fixture. Returns a running EnvTest instance</p> <p>Yields:</p> Type Description <code>EnvTest</code> <p>Generator[EnvTest, None, None]: The running EnvTest instance</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@pytest.fixture(scope=\"session\")\ndef session_kubernetes_env() -&gt; Generator[EnvTest, None, None]:\n    \"\"\"Session scoped version of the above fixture. Returns a running\n    EnvTest instance\n\n    Yields:\n        Generator[EnvTest, None, None]: The running EnvTest instance\n    \"\"\"\n    with yield_kubernetes_env() as env:\n        yield env\n</code></pre>"},{"location":"api_references/#kubernetes_lite.envtest.pytest.yield_kubernetes_env","title":"<code>yield_kubernetes_env()</code>","text":"<p>Context manager to yield a running EnvTest instance and handle stopping it after the fact</p> <p>Yields:</p> Type Description <code>EnvTest</code> <p>Generator[EnvTest, None, None]: The running EnvTest instance</p> Source code in <code>kubernetes_lite/envtest/pytest.py</code> <pre><code>@contextmanager\ndef yield_kubernetes_env() -&gt; Generator[EnvTest, None, None]:\n    \"\"\"Context manager to yield a running EnvTest instance and handle\n    stopping it after the fact\n\n    Yields:\n        Generator[EnvTest, None, None]: The running EnvTest instance\n    \"\"\"\n    env = EnvTest()\n    env.start()\n    yield env\n    env.stop()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors","title":"<code>errors</code>","text":"<p>This module contains a list of possible kubernetes errors and helper functions to convert go_wrapper errors into their respective python classes</p>"},{"location":"api_references/#kubernetes_lite.errors.AlreadyExistsError","title":"<code>AlreadyExistsError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class AlreadyExistsError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.BadRequestError","title":"<code>BadRequestError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class BadRequestError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ClientTimeoutError","title":"<code>ClientTimeoutError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason. This class corresponds to the general TimeoutError</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ClientTimeoutError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason.\n    This class corresponds to the general TimeoutError\n    \"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ConflictError","title":"<code>ConflictError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ConflictError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ExpiredError","title":"<code>ExpiredError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ExpiredError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ForbiddenError","title":"<code>ForbiddenError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ForbiddenError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.GoneError","title":"<code>GoneError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class GoneError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.InternalErrorError","title":"<code>InternalErrorError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class InternalErrorError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.InvalidError","title":"<code>InvalidError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class InvalidError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.MethodNotAllowedError","title":"<code>MethodNotAllowedError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class MethodNotAllowedError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.NotAcceptableError","title":"<code>NotAcceptableError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class NotAcceptableError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.NotFoundError","title":"<code>NotFoundError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class NotFoundError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.RequestEntityTooLargeError","title":"<code>RequestEntityTooLargeError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class RequestEntityTooLargeError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ServerTimeoutError","title":"<code>ServerTimeoutError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ServerTimeoutError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.ServiceUnavailableError","title":"<code>ServiceUnavailableError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class ServiceUnavailableError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.StoreReadErrorError","title":"<code>StoreReadErrorError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class StoreReadErrorError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.TooManyRequestsError","title":"<code>TooManyRequestsError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class TooManyRequestsError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.UnauthorizedError","title":"<code>UnauthorizedError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class UnauthorizedError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.UnknownError","title":"<code>UnknownError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class UnknownError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.UnsupportedMediaTypeError","title":"<code>UnsupportedMediaTypeError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>class UnsupportedMediaTypeError(RuntimeError):\n    \"\"\"See https://pkg.go.dev/k8s.io/apimachinery/pkg/apis/meta/v1#StatusReason\"\"\"\n</code></pre>"},{"location":"api_references/#kubernetes_lite.errors.wrap_kube_error","title":"<code>wrap_kube_error(func)</code>","text":"<p>Wrap a a function so that generic gopy RuntimeErrors are turned into the appropriate kubernetes error type.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>The function to wrap</p> required <p>Returns:</p> Name Type Description <code>Callable</code> <code>Callable[KUBE_PARAM_TYPE, KUBE_RETURN_TYPE]</code> <p>The wrapped function</p> Source code in <code>kubernetes_lite/errors.py</code> <pre><code>def wrap_kube_error(func: Callable[KUBE_PARAM_TYPE, KUBE_RETURN_TYPE]) -&gt; Callable[KUBE_PARAM_TYPE, KUBE_RETURN_TYPE]:\n    \"\"\"Wrap a a function so that generic gopy RuntimeErrors\n    are turned into the appropriate kubernetes error type.\n\n    Args:\n        func (Callable): The function to wrap\n\n    Returns:\n        Callable: The wrapped function\n    \"\"\"\n\n    def wrapped_callable(*args: KUBE_PARAM_TYPE.args, **kwargs: KUBE_PARAM_TYPE.kwargs) -&gt; KUBE_RETURN_TYPE:\n        try:\n            return func(*args, **kwargs)\n        except RuntimeError as exc:\n            # If the error has an args attribute then try to parse it into one of\n            # the above classes\n            if len(exc.args) &gt; 0:\n                # Split the string at : to try to extract the message type. This is\n                # defined by KubernetesErrorSep in the go code\n                message = str(exc.args[0])\n                split_message = message.split(\":\")\n\n                # Use the first part of the message to map to the exception type\n                if split_message[0] in EXCEPTION_MAPPING:\n                    new_exc_type = EXCEPTION_MAPPING[split_message[0]]\n\n                    # Recreate the unsplit message with out the parsed bit\n                    exc_message = \":\".join(split_message[1:])\n                    raise new_exc_type(exc_message) from None\n            raise exc\n\n    return wrapped_callable\n</code></pre>"},{"location":"api_references/#kubernetes_lite.serialization","title":"<code>serialization</code>","text":"<p>This module contains helper functions to serialize python objects into go Slice_bytes and back</p>"},{"location":"api_references/#kubernetes_lite.serialization.deserialize_from_go","title":"<code>deserialize_from_go(obj)</code>","text":"<p>Deserialize a Slice_byte object from go into a python dictionary</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Slice_byte</code> <p>The go object to deserialize</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The deserialized object as a dictionary</p> Source code in <code>kubernetes_lite/serialization.py</code> <pre><code>def deserialize_from_go(obj: Slice_byte) -&gt; dict:\n    \"\"\"Deserialize a Slice_byte object from go into a python dictionary\n\n    Args:\n        obj (Slice_byte): The go object to deserialize\n\n    Returns:\n        dict: The deserialized object as a dictionary\n    \"\"\"\n    return deserialize_result(bytes(obj))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.serialization.deserialize_result","title":"<code>deserialize_result(obj)</code>","text":"<p>Deserialize an object using orjson into a python dictionary</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>bytes</code> <p>The bytes object to deserialize</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The deserialized object as a dictionary</p> Source code in <code>kubernetes_lite/serialization.py</code> <pre><code>def deserialize_result(obj: bytes) -&gt; dict:\n    \"\"\"Deserialize an object using orjson into a python dictionary\n\n    Args:\n        obj (bytes): The bytes object to deserialize\n\n    Returns:\n        dict: The deserialized object as a dictionary\n    \"\"\"\n    return orjson.loads(obj)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.serialization.serialize","title":"<code>serialize(obj)</code>","text":"<p>Serialize a python object using orjson into bytes</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to serialize</p> required <p>Returns:</p> Name Type Description <code>bytes</code> <code>bytes</code> <p>The return bytes data</p> Source code in <code>kubernetes_lite/serialization.py</code> <pre><code>def serialize(obj: Any) -&gt; bytes:\n    \"\"\"Serialize a python object using orjson into bytes\n\n    Args:\n        obj (Any): The object to serialize\n\n    Returns:\n        bytes: The return bytes data\n    \"\"\"\n    if isinstance(obj, bytes):\n        return obj\n    return orjson.dumps(obj)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.serialization.serialize_to_go","title":"<code>serialize_to_go(obj)</code>","text":"<p>Serialize a python object into a go object</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>The object to serialize</p> required <p>Returns:</p> Name Type Description <code>Slice_byte</code> <code>Slice_byte</code> <p>Pointer to the go object</p> Source code in <code>kubernetes_lite/serialization.py</code> <pre><code>def serialize_to_go(obj: Any) -&gt; Slice_byte:\n    \"\"\"Serialize a python object into a go object\n\n    Args:\n        obj (Any): The object to serialize\n\n    Returns:\n        Slice_byte: Pointer to the go object\n    \"\"\"\n    if isinstance(obj, Slice_byte):\n        return obj\n    if isinstance(obj, BytesIO):\n        obj = obj.read()\n    # Don't use Slice_byte(&lt;&gt;) since its slow!\n    handle = Slice_byte_from_bytes(serialize(obj))\n    return Slice_byte(handle=handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest","title":"<code>setup_envtest</code>","text":"<p>This module contains functions to run setup-envtest. See https://pkg.go.dev/sigs.k8s.io/controller-runtime/tools/setup-envtest for more information regarding arguments</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.internal_run_setup_envtest_command","title":"<code>internal_run_setup_envtest_command(*args, stdin=b'')</code>","text":"<p>This function runs the underlying setup_envtest command with a provided args and stdin.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>list[str]</code> <p>The arguments to pass to setup-envtest</p> <code>()</code> <code>stdin</code> <code>bytes</code> <p>The stdin data to send to setup-envtest. Defaults to b\"\".</p> <code>b''</code> <p>Returns:</p> Name Type Description <code>SetupEnvTestResult</code> <code>SetupEnvTestResult</code> <p>A parsed dataclass containing the returned error and captured stdout/stderr</p> Source code in <code>kubernetes_lite/setup_envtest/command.py</code> <pre><code>def internal_run_setup_envtest_command(*args: list[str], stdin: bytes = b\"\") -&gt; SetupEnvTestResult:\n    \"\"\"This function runs the underlying setup_envtest command with a provided args and stdin.\n\n    Args:\n        *args (list[str]): The arguments to pass to setup-envtest\n        stdin (bytes, optional): The stdin data to send to setup-envtest. Defaults to b\"\".\n\n    Returns:\n        SetupEnvTestResult: A parsed dataclass containing the returned error and captured stdout/stderr\n    \"\"\"\n    # Convert input args to go slices\n    go_stdin = serialize_to_go(stdin)\n    go_args = serialize_to_go([\"python3 -m kubernetes_lite.setup_envtest\", *args])\n\n    # Run setup env test\n    setup_result_raw = SetupEnvTest(go_stdin, go_args)\n\n    # Deserialize the result\n    return SetupEnvTestResult.from_go(setup_result_raw)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.run_setup_envtest_command","title":"<code>run_setup_envtest_command(log=None)</code>","text":"<p>Function to handle running the setup_envtest command as if the user ran this module directly. It handles reading from the process args/stdin and passing them to internal_run_setup_envtest_command. This function handles logging the result and exiting with the correct exit code</p> <p>Parameters:</p> Name Type Description Default <code>log</code> <code>Logger | None</code> <p>Logger to use for printing setup_envtest messages. Defaults to None.</p> <code>None</code> Source code in <code>kubernetes_lite/setup_envtest/command.py</code> <pre><code>def run_setup_envtest_command(log: Logger | None = None):\n    \"\"\"Function to handle running the setup_envtest command as if the user ran this module\n    directly. It handles reading from the process args/stdin and passing them to\n    internal_run_setup_envtest_command. This function handles logging the result and\n    exiting with the correct exit code\n\n    Args:\n        log (Logger | None, optional): Logger to use for printing setup_envtest messages.\n            Defaults to None.\n    \"\"\"\n    if not log:\n        log = logging.getLogger(\"setup-envtest\")\n\n    args = copy.deepcopy(sys.argv)\n\n    # If there are args in sys.args then check to see if the module was included. If so remove the first item\n    if len(args) &gt; 0:\n        current_file_path = Path(__file__)\n        if str(current_file_path.parent) in args[0]:\n            args.pop(0)\n\n    # Read stdin into a buffer\n    stdin_available_list, _, _ = select.select([sys.stdin], [], [], 0.1)\n    stdin_data = \"\"\n    if len(stdin_available_list) &gt; 0:\n        stdin_byte = sys.stdin.read(1)\n        stdin_data = stdin_byte\n        while len(stdin_byte) &gt; 0:\n            stdin_byte = sys.stdin.read(1)\n            stdin_data += stdin_byte\n\n    setup_result = internal_run_setup_envtest_command(*args, stdin=stdin_data.encode(\"utf-8\"))\n\n    # Log results messages without raising the exception\n    setup_result.log_and_raise(log)\n\n    # If we parsed and exit code from the logs then exit\n    if setup_result.exit_data and setup_result.exit_data.code:\n        sys.exit(setup_result.exit_data.code)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.__main__","title":"<code>__main__</code>","text":"<p>This module runs the setup-envtest command from python as if the user ran setuptest-env directly</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.command","title":"<code>command</code>","text":"<p>This module contains functions to run setup-envtest commands</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.command.internal_run_setup_envtest_command","title":"<code>internal_run_setup_envtest_command(*args, stdin=b'')</code>","text":"<p>This function runs the underlying setup_envtest command with a provided args and stdin.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>list[str]</code> <p>The arguments to pass to setup-envtest</p> <code>()</code> <code>stdin</code> <code>bytes</code> <p>The stdin data to send to setup-envtest. Defaults to b\"\".</p> <code>b''</code> <p>Returns:</p> Name Type Description <code>SetupEnvTestResult</code> <code>SetupEnvTestResult</code> <p>A parsed dataclass containing the returned error and captured stdout/stderr</p> Source code in <code>kubernetes_lite/setup_envtest/command.py</code> <pre><code>def internal_run_setup_envtest_command(*args: list[str], stdin: bytes = b\"\") -&gt; SetupEnvTestResult:\n    \"\"\"This function runs the underlying setup_envtest command with a provided args and stdin.\n\n    Args:\n        *args (list[str]): The arguments to pass to setup-envtest\n        stdin (bytes, optional): The stdin data to send to setup-envtest. Defaults to b\"\".\n\n    Returns:\n        SetupEnvTestResult: A parsed dataclass containing the returned error and captured stdout/stderr\n    \"\"\"\n    # Convert input args to go slices\n    go_stdin = serialize_to_go(stdin)\n    go_args = serialize_to_go([\"python3 -m kubernetes_lite.setup_envtest\", *args])\n\n    # Run setup env test\n    setup_result_raw = SetupEnvTest(go_stdin, go_args)\n\n    # Deserialize the result\n    return SetupEnvTestResult.from_go(setup_result_raw)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.command.run_setup_envtest_command","title":"<code>run_setup_envtest_command(log=None)</code>","text":"<p>Function to handle running the setup_envtest command as if the user ran this module directly. It handles reading from the process args/stdin and passing them to internal_run_setup_envtest_command. This function handles logging the result and exiting with the correct exit code</p> <p>Parameters:</p> Name Type Description Default <code>log</code> <code>Logger | None</code> <p>Logger to use for printing setup_envtest messages. Defaults to None.</p> <code>None</code> Source code in <code>kubernetes_lite/setup_envtest/command.py</code> <pre><code>def run_setup_envtest_command(log: Logger | None = None):\n    \"\"\"Function to handle running the setup_envtest command as if the user ran this module\n    directly. It handles reading from the process args/stdin and passing them to\n    internal_run_setup_envtest_command. This function handles logging the result and\n    exiting with the correct exit code\n\n    Args:\n        log (Logger | None, optional): Logger to use for printing setup_envtest messages.\n            Defaults to None.\n    \"\"\"\n    if not log:\n        log = logging.getLogger(\"setup-envtest\")\n\n    args = copy.deepcopy(sys.argv)\n\n    # If there are args in sys.args then check to see if the module was included. If so remove the first item\n    if len(args) &gt; 0:\n        current_file_path = Path(__file__)\n        if str(current_file_path.parent) in args[0]:\n            args.pop(0)\n\n    # Read stdin into a buffer\n    stdin_available_list, _, _ = select.select([sys.stdin], [], [], 0.1)\n    stdin_data = \"\"\n    if len(stdin_available_list) &gt; 0:\n        stdin_byte = sys.stdin.read(1)\n        stdin_data = stdin_byte\n        while len(stdin_byte) &gt; 0:\n            stdin_byte = sys.stdin.read(1)\n            stdin_data += stdin_byte\n\n    setup_result = internal_run_setup_envtest_command(*args, stdin=stdin_data.encode(\"utf-8\"))\n\n    # Log results messages without raising the exception\n    setup_result.log_and_raise(log)\n\n    # If we parsed and exit code from the logs then exit\n    if setup_result.exit_data and setup_result.exit_data.code:\n        sys.exit(setup_result.exit_data.code)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.exit_data","title":"<code>exit_data</code>","text":"<p>This module contains helper dataclasses/functions for parsing exit data information from setupenv-test</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.exit_data.ExitData","title":"<code>ExitData</code>  <code>dataclass</code>","text":"<p>ExitData dataclass contains the exit code and message for a setup-envtest command run</p> <p>Attributes:</p> Name Type Description <code>code</code> <code>int</code> <p>The exit code that was parsed</p> <code>message</code> <code>str | None</code> <p>An optional string message if one was extracted</p> Source code in <code>kubernetes_lite/setup_envtest/exit_data.py</code> <pre><code>@dataclass\nclass ExitData:\n    \"\"\"ExitData dataclass contains the exit code and message for a setup-envtest\n    command run\n\n    Attributes:\n        code int: The exit code that was parsed\n        message (str | None): An optional string message if one was extracted\n    \"\"\"\n\n    code: int\n    message: str | None\n\n    @classmethod\n    def from_string(cls, error: str) -&gt; \"ExitData|None\":\n        \"\"\"Attempt to parse a error string into a ExitData dataclass\n\n        Returns:\n            ExitData|None: The parsed ExitData or None if one couldn't\n                be extracted\n        \"\"\"\n        # Attempt to match the regex\n        match = EXIT_CODE_REGEX.match(error)\n        if not match:\n            return\n\n        matches = match.groupdict()\n        exit_code = matches.get(\"code\", None)\n        message = matches.get(\"text\", None).strip()\n        if message:\n            message = None\n\n        if exit_code:\n            try:\n                exit_code = int(exit_code)\n            except ValueError:\n                return\n\n        return cls(code=exit_code, message=message)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.exit_data.ExitData.from_string","title":"<code>from_string(error)</code>  <code>classmethod</code>","text":"<p>Attempt to parse a error string into a ExitData dataclass</p> <p>Returns:</p> Type Description <code>ExitData | None</code> <p>ExitData|None: The parsed ExitData or None if one couldn't be extracted</p> Source code in <code>kubernetes_lite/setup_envtest/exit_data.py</code> <pre><code>@classmethod\ndef from_string(cls, error: str) -&gt; \"ExitData|None\":\n    \"\"\"Attempt to parse a error string into a ExitData dataclass\n\n    Returns:\n        ExitData|None: The parsed ExitData or None if one couldn't\n            be extracted\n    \"\"\"\n    # Attempt to match the regex\n    match = EXIT_CODE_REGEX.match(error)\n    if not match:\n        return\n\n    matches = match.groupdict()\n    exit_code = matches.get(\"code\", None)\n    message = matches.get(\"text\", None).strip()\n    if message:\n        message = None\n\n    if exit_code:\n        try:\n            exit_code = int(exit_code)\n        except ValueError:\n            return\n\n    return cls(code=exit_code, message=message)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result","title":"<code>result</code>","text":"<p>This module contains a dataclass used in processing the result from setup-envtest</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result.SetupEnvTestResult","title":"<code>SetupEnvTestResult</code>  <code>dataclass</code>","text":"<p>SetupEnvTestResult contains information regarding the result of a setup-envtest run. This includes the captured stdout/stderr as well as the exit information</p> <p>Attributes:</p> Name Type Description <code>stderr</code> <code>str | None</code> <p>the captured stderr data if some was captured</p> <code>stdout</code> <code>str | None</code> <p>the captured stdout data if some was captured</p> <code>error</code> <code>str | None</code> <p>the serialized error data if one was returned</p> Source code in <code>kubernetes_lite/setup_envtest/result.py</code> <pre><code>@dataclass\nclass SetupEnvTestResult:\n    \"\"\"SetupEnvTestResult contains information regarding the result of a setup-envtest\n    run. This includes the captured stdout/stderr as well as the exit information\n\n    Attributes:\n        stderr (str | None): the captured stderr data if some was captured\n        stdout (str | None): the captured stdout data if some was captured\n        error (str | None): the serialized error data if one was returned\n    \"\"\"\n\n    stderr: str | None = None\n    stdout: str | None = None\n    error: str | None = None\n\n    @cached_property\n    def exit_data(self) -&gt; ExitData | None:\n        \"\"\"The parsed ExitData instance for this result\n\n        Returns:\n            ExitData | None: The ExitData object if one could be generated. Else None\n        \"\"\"\n        if not self.error:\n            return\n        return ExitData.from_string(self.error)\n\n    @cached_property\n    def is_multi_output(self) -&gt; bool:\n        \"\"\"Helper property used when printing the result to determine if\n        multiple ==&lt;&gt;== header tags are needed\n\n        Returns:\n            bool: If more then one of stdout/stderr/error was provided\n        \"\"\"\n        return (\n            sum(\n                [\n                    bool(self.stdout),\n                    bool(self.stderr),\n                    bool(self.exit_data.message if self.exit_data else False),\n                ]\n            )\n            &gt; 1\n        )\n\n    def log_and_raise(self, log: Logger, raise_exc: bool = False):\n        \"\"\"Helper function to print the results to a log and raise an exception\n        if the command was not successful\n\n        Args:\n            log (Logger): The logger to print result information to\n            raise_exc (bool, optional): If an exception should be raised if the result was\n                invalid. Defaults to False.\n        \"\"\"\n        should_raise_exc: bool = False\n        if self.exit_data and self.exit_data.message:\n            if self.is_multi_output:\n                log.error(\"==Error==\")\n            log.error(self.exit_data.message)\n            should_raise_exc = True\n        if self.stderr:\n            if self.is_multi_output:\n                log.info(\"==StdErr==\")\n            log.error(self.stderr)\n            should_raise_exc = True\n        if self.stdout:\n            if self.is_multi_output:\n                log.info(\"==StdOut==\")\n            log.info(self.stdout)\n\n        if should_raise_exc and raise_exc:\n            raise RuntimeError(\"Failed to run SetupEvTet\")\n\n    @classmethod\n    def from_go(cls, raw_data: Slice_byte) -&gt; \"SetupEnvTestResult\":\n        \"\"\"Parse a SetupEnvTestResult object from a raw golang Slice_byte object\n\n        Args:\n            raw_data (Slice_byte): The raw data from go\n\n        Returns:\n            SetupEnvTestResult: the parsed result object\n        \"\"\"\n        result_dict = deserialize_from_go(raw_data)\n\n        return cls(**result_dict)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result.SetupEnvTestResult.exit_data","title":"<code>exit_data</code>  <code>cached</code> <code>property</code>","text":"<p>The parsed ExitData instance for this result</p> <p>Returns:</p> Type Description <code>ExitData | None</code> <p>ExitData | None: The ExitData object if one could be generated. Else None</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result.SetupEnvTestResult.is_multi_output","title":"<code>is_multi_output</code>  <code>cached</code> <code>property</code>","text":"<p>Helper property used when printing the result to determine if multiple ==&lt;&gt;== header tags are needed</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If more then one of stdout/stderr/error was provided</p>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result.SetupEnvTestResult.from_go","title":"<code>from_go(raw_data)</code>  <code>classmethod</code>","text":"<p>Parse a SetupEnvTestResult object from a raw golang Slice_byte object</p> <p>Parameters:</p> Name Type Description Default <code>raw_data</code> <code>Slice_byte</code> <p>The raw data from go</p> required <p>Returns:</p> Name Type Description <code>SetupEnvTestResult</code> <code>SetupEnvTestResult</code> <p>the parsed result object</p> Source code in <code>kubernetes_lite/setup_envtest/result.py</code> <pre><code>@classmethod\ndef from_go(cls, raw_data: Slice_byte) -&gt; \"SetupEnvTestResult\":\n    \"\"\"Parse a SetupEnvTestResult object from a raw golang Slice_byte object\n\n    Args:\n        raw_data (Slice_byte): The raw data from go\n\n    Returns:\n        SetupEnvTestResult: the parsed result object\n    \"\"\"\n    result_dict = deserialize_from_go(raw_data)\n\n    return cls(**result_dict)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.setup_envtest.result.SetupEnvTestResult.log_and_raise","title":"<code>log_and_raise(log, raise_exc=False)</code>","text":"<p>Helper function to print the results to a log and raise an exception if the command was not successful</p> <p>Parameters:</p> Name Type Description Default <code>log</code> <code>Logger</code> <p>The logger to print result information to</p> required <code>raise_exc</code> <code>bool</code> <p>If an exception should be raised if the result was invalid. Defaults to False.</p> <code>False</code> Source code in <code>kubernetes_lite/setup_envtest/result.py</code> <pre><code>def log_and_raise(self, log: Logger, raise_exc: bool = False):\n    \"\"\"Helper function to print the results to a log and raise an exception\n    if the command was not successful\n\n    Args:\n        log (Logger): The logger to print result information to\n        raise_exc (bool, optional): If an exception should be raised if the result was\n            invalid. Defaults to False.\n    \"\"\"\n    should_raise_exc: bool = False\n    if self.exit_data and self.exit_data.message:\n        if self.is_multi_output:\n            log.error(\"==Error==\")\n        log.error(self.exit_data.message)\n        should_raise_exc = True\n    if self.stderr:\n        if self.is_multi_output:\n            log.info(\"==StdErr==\")\n        log.error(self.stderr)\n        should_raise_exc = True\n    if self.stdout:\n        if self.is_multi_output:\n            log.info(\"==StdOut==\")\n        log.info(self.stdout)\n\n    if should_raise_exc and raise_exc:\n        raise RuntimeError(\"Failed to run SetupEvTet\")\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper","title":"<code>wrapper</code>","text":""},{"location":"api_references/#kubernetes_lite.wrapper.client","title":"<code>client</code>","text":""},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedDynamicClient","title":"<code>WrappedDynamicClient</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>Simple dynamic client interface that allows users to create a Resource Interface</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>class WrappedDynamicClient(go.GoClass):\n\t\"\"\"Simple dynamic client interface that allows users to create a Resource Interface\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef Resource(self, apiVersion, kind):\n\t\t\"\"\"Resource(str apiVersion, str kind) object, str\"\"\"\n\t\treturn WrappedNamespaceableResourceInterface(handle=_wrapper.client_WrappedDynamicClient_Resource(self.handle, apiVersion, kind))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedDynamicClient.Resource","title":"<code>Resource(apiVersion, kind)</code>","text":"<p>Resource(str apiVersion, str kind) object, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Resource(self, apiVersion, kind):\n\t\"\"\"Resource(str apiVersion, str kind) object, str\"\"\"\n\treturn WrappedNamespaceableResourceInterface(handle=_wrapper.client_WrappedDynamicClient_Resource(self.handle, apiVersion, kind))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedDynamicClient.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface","title":"<code>WrappedNamespaceableResourceInterface</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>WrappedNamespaceableResourceInterface is a wrapper around the ResourceInterface for interacting with cluster wide resources. This interface can also be namespace scoped with the Namespace method.</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>class WrappedNamespaceableResourceInterface(go.GoClass):\n\t\"\"\"WrappedNamespaceableResourceInterface is a wrapper around the ResourceInterface for interacting with\\ncluster wide resources. This interface can also be namespace scoped with the Namespace method.\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef Apply(self, name, obj, opts, *args):\n\t\t\"\"\"Apply(str name, []int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Apply(self.handle, name, obj.handle, opts.handle, subresources.handle))\n\tdef ApplyStatus(self, name, obj, opts):\n\t\t\"\"\"ApplyStatus(str name, []int obj, []int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_ApplyStatus(self.handle, name, obj.handle, opts.handle))\n\tdef Create(self, obj, opts, *args):\n\t\t\"\"\"Create([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Create(self.handle, obj.handle, opts.handle, subresources.handle))\n\tdef Delete(self, name, opts, *args):\n\t\t\"\"\"Delete(str name, []int opts, []str subresources) str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn _wrapper.client_WrappedNamespaceableResourceInterface_Delete(self.handle, name, opts.handle, subresources.handle)\n\tdef DeleteCollection(self, opts, listOptions):\n\t\t\"\"\"DeleteCollection([]int opts, []int listOptions) str\"\"\"\n\t\treturn _wrapper.client_WrappedNamespaceableResourceInterface_DeleteCollection(self.handle, opts.handle, listOptions.handle)\n\tdef Get(self, name, opts, *args):\n\t\t\"\"\"Get(str name, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Get(self.handle, name, opts.handle, subresources.handle))\n\tdef List(self, opts):\n\t\t\"\"\"List([]int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_List(self.handle, opts.handle))\n\tdef Namespace(self, arg_0):\n\t\t\"\"\"Namespace(str) object\"\"\"\n\t\treturn WrappedResourceInterface(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Namespace(self.handle, arg_0))\n\tdef Patch(self, name, pt, data, opts, *args):\n\t\t\"\"\"Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Patch(self.handle, name, pt, data.handle, opts.handle, subresources.handle))\n\tdef Update(self, obj, opts, *args):\n\t\t\"\"\"Update([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Update(self.handle, obj.handle, opts.handle, subresources.handle))\n\tdef UpdateStatus(self, obj, opts):\n\t\t\"\"\"UpdateStatus([]int obj, []int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_UpdateStatus(self.handle, obj.handle, opts.handle))\n\tdef Watch(self, opts):\n\t\t\"\"\"Watch([]int opts) object, str\"\"\"\n\t\treturn WrappedWatchInterface(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Watch(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Apply","title":"<code>Apply(name, obj, opts, *args)</code>","text":"<p>Apply(str name, []int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Apply(self, name, obj, opts, *args):\n\t\"\"\"Apply(str name, []int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Apply(self.handle, name, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.ApplyStatus","title":"<code>ApplyStatus(name, obj, opts)</code>","text":"<p>ApplyStatus(str name, []int obj, []int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def ApplyStatus(self, name, obj, opts):\n\t\"\"\"ApplyStatus(str name, []int obj, []int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_ApplyStatus(self.handle, name, obj.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Create","title":"<code>Create(obj, opts, *args)</code>","text":"<p>Create([]int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Create(self, obj, opts, *args):\n\t\"\"\"Create([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Create(self.handle, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Delete","title":"<code>Delete(name, opts, *args)</code>","text":"<p>Delete(str name, []int opts, []str subresources) str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Delete(self, name, opts, *args):\n\t\"\"\"Delete(str name, []int opts, []str subresources) str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn _wrapper.client_WrappedNamespaceableResourceInterface_Delete(self.handle, name, opts.handle, subresources.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.DeleteCollection","title":"<code>DeleteCollection(opts, listOptions)</code>","text":"<p>DeleteCollection([]int opts, []int listOptions) str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def DeleteCollection(self, opts, listOptions):\n\t\"\"\"DeleteCollection([]int opts, []int listOptions) str\"\"\"\n\treturn _wrapper.client_WrappedNamespaceableResourceInterface_DeleteCollection(self.handle, opts.handle, listOptions.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Get","title":"<code>Get(name, opts, *args)</code>","text":"<p>Get(str name, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Get(self, name, opts, *args):\n\t\"\"\"Get(str name, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Get(self.handle, name, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.List","title":"<code>List(opts)</code>","text":"<p>List([]int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def List(self, opts):\n\t\"\"\"List([]int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_List(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Namespace","title":"<code>Namespace(arg_0)</code>","text":"<p>Namespace(str) object</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Namespace(self, arg_0):\n\t\"\"\"Namespace(str) object\"\"\"\n\treturn WrappedResourceInterface(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Namespace(self.handle, arg_0))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Patch","title":"<code>Patch(name, pt, data, opts, *args)</code>","text":"<p>Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Patch(self, name, pt, data, opts, *args):\n\t\"\"\"Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Patch(self.handle, name, pt, data.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Update","title":"<code>Update(obj, opts, *args)</code>","text":"<p>Update([]int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Update(self, obj, opts, *args):\n\t\"\"\"Update([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Update(self.handle, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.UpdateStatus","title":"<code>UpdateStatus(obj, opts)</code>","text":"<p>UpdateStatus([]int obj, []int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def UpdateStatus(self, obj, opts):\n\t\"\"\"UpdateStatus([]int obj, []int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedNamespaceableResourceInterface_UpdateStatus(self.handle, obj.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.Watch","title":"<code>Watch(opts)</code>","text":"<p>Watch([]int opts) object, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Watch(self, opts):\n\t\"\"\"Watch([]int opts) object, str\"\"\"\n\treturn WrappedWatchInterface(handle=_wrapper.client_WrappedNamespaceableResourceInterface_Watch(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedNamespaceableResourceInterface.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface","title":"<code>WrappedResourceInterface</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>The WrappedResourceInterface is designed to mirror client-go's ResourceInterface with one major difference. Instead of accepting and returning pointers to the various apimachinery types/objects it handles everything though binary buffers (*uint16) pointers which greatly simplifies the CGO function call</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>class WrappedResourceInterface(go.GoClass):\n\t\"\"\"The WrappedResourceInterface is designed to mirror client-go's ResourceInterface with one major\\ndifference. Instead of accepting and returning pointers to the various apimachinery types/objects\\nit handles everything though binary buffers (*uint16) pointers which greatly simplifies the CGO\\nfunction call\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef Apply(self, name, obj, opts, *args):\n\t\t\"\"\"Apply(str name, []int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Apply(self.handle, name, obj.handle, opts.handle, subresources.handle))\n\tdef ApplyStatus(self, name, obj, opts):\n\t\t\"\"\"ApplyStatus(str name, []int obj, []int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_ApplyStatus(self.handle, name, obj.handle, opts.handle))\n\tdef Create(self, obj, opts, *args):\n\t\t\"\"\"Create([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Create(self.handle, obj.handle, opts.handle, subresources.handle))\n\tdef Delete(self, name, opts, *args):\n\t\t\"\"\"Delete(str name, []int opts, []str subresources) str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn _wrapper.client_WrappedResourceInterface_Delete(self.handle, name, opts.handle, subresources.handle)\n\tdef DeleteCollection(self, opts, listOptions):\n\t\t\"\"\"DeleteCollection([]int opts, []int listOptions) str\"\"\"\n\t\treturn _wrapper.client_WrappedResourceInterface_DeleteCollection(self.handle, opts.handle, listOptions.handle)\n\tdef Get(self, name, opts, *args):\n\t\t\"\"\"Get(str name, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Get(self.handle, name, opts.handle, subresources.handle))\n\tdef List(self, opts):\n\t\t\"\"\"List([]int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_List(self.handle, opts.handle))\n\tdef Patch(self, name, pt, data, opts, *args):\n\t\t\"\"\"Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Patch(self.handle, name, pt, data.handle, opts.handle, subresources.handle))\n\tdef Update(self, obj, opts, *args):\n\t\t\"\"\"Update([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\t\tsubresources = go.Slice_string(args)\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Update(self.handle, obj.handle, opts.handle, subresources.handle))\n\tdef UpdateStatus(self, obj, opts):\n\t\t\"\"\"UpdateStatus([]int obj, []int opts) []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_UpdateStatus(self.handle, obj.handle, opts.handle))\n\tdef Watch(self, opts):\n\t\t\"\"\"Watch([]int opts) object, str\"\"\"\n\t\treturn WrappedWatchInterface(handle=_wrapper.client_WrappedResourceInterface_Watch(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Apply","title":"<code>Apply(name, obj, opts, *args)</code>","text":"<p>Apply(str name, []int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Apply(self, name, obj, opts, *args):\n\t\"\"\"Apply(str name, []int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Apply(self.handle, name, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.ApplyStatus","title":"<code>ApplyStatus(name, obj, opts)</code>","text":"<p>ApplyStatus(str name, []int obj, []int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def ApplyStatus(self, name, obj, opts):\n\t\"\"\"ApplyStatus(str name, []int obj, []int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_ApplyStatus(self.handle, name, obj.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Create","title":"<code>Create(obj, opts, *args)</code>","text":"<p>Create([]int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Create(self, obj, opts, *args):\n\t\"\"\"Create([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Create(self.handle, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Delete","title":"<code>Delete(name, opts, *args)</code>","text":"<p>Delete(str name, []int opts, []str subresources) str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Delete(self, name, opts, *args):\n\t\"\"\"Delete(str name, []int opts, []str subresources) str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn _wrapper.client_WrappedResourceInterface_Delete(self.handle, name, opts.handle, subresources.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.DeleteCollection","title":"<code>DeleteCollection(opts, listOptions)</code>","text":"<p>DeleteCollection([]int opts, []int listOptions) str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def DeleteCollection(self, opts, listOptions):\n\t\"\"\"DeleteCollection([]int opts, []int listOptions) str\"\"\"\n\treturn _wrapper.client_WrappedResourceInterface_DeleteCollection(self.handle, opts.handle, listOptions.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Get","title":"<code>Get(name, opts, *args)</code>","text":"<p>Get(str name, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Get(self, name, opts, *args):\n\t\"\"\"Get(str name, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Get(self.handle, name, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.List","title":"<code>List(opts)</code>","text":"<p>List([]int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def List(self, opts):\n\t\"\"\"List([]int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_List(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Patch","title":"<code>Patch(name, pt, data, opts, *args)</code>","text":"<p>Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Patch(self, name, pt, data, opts, *args):\n\t\"\"\"Patch(str name, str pt, []int data, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Patch(self.handle, name, pt, data.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Update","title":"<code>Update(obj, opts, *args)</code>","text":"<p>Update([]int obj, []int opts, []str subresources) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Update(self, obj, opts, *args):\n\t\"\"\"Update([]int obj, []int opts, []str subresources) []int, str\"\"\"\n\tsubresources = go.Slice_string(args)\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_Update(self.handle, obj.handle, opts.handle, subresources.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.UpdateStatus","title":"<code>UpdateStatus(obj, opts)</code>","text":"<p>UpdateStatus([]int obj, []int opts) []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def UpdateStatus(self, obj, opts):\n\t\"\"\"UpdateStatus([]int obj, []int opts) []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedResourceInterface_UpdateStatus(self.handle, obj.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.Watch","title":"<code>Watch(opts)</code>","text":"<p>Watch([]int opts) object, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Watch(self, opts):\n\t\"\"\"Watch([]int opts) object, str\"\"\"\n\treturn WrappedWatchInterface(handle=_wrapper.client_WrappedResourceInterface_Watch(self.handle, opts.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedResourceInterface.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedWatchInterface","title":"<code>WrappedWatchInterface</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>WrappedWatchInterface allows CGO applications to iterate over a channel</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>class WrappedWatchInterface(go.GoClass):\n\t\"\"\"WrappedWatchInterface allows CGO applications to iterate over a channel\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef Next(self):\n\t\t\"\"\"Next() []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.client_WrappedWatchInterface_Next(self.handle))\n\tdef Stop(self, goRun=False):\n\t\t\"\"\"Stop() \"\"\"\n\t\t_wrapper.client_WrappedWatchInterface_Stop(self.handle, goRun)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedWatchInterface.Next","title":"<code>Next()</code>","text":"<p>Next() []int, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Next(self):\n\t\"\"\"Next() []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.client_WrappedWatchInterface_Next(self.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedWatchInterface.Stop","title":"<code>Stop(goRun=False)</code>","text":"<p>Stop()</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Stop(self, goRun=False):\n\t\"\"\"Stop() \"\"\"\n\t_wrapper.client_WrappedWatchInterface_Stop(self.handle, goRun)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.WrappedWatchInterface.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.EmptyEvent","title":"<code>EmptyEvent()</code>","text":"<p>EmptyEvent Gets Go Variable: client.EmptyEvent</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def EmptyEvent():\n\t\"\"\"\n\tEmptyEvent Gets Go Variable: client.EmptyEvent\n\n\t\"\"\"\n\treturn go.watch_Event(handle=_wrapper.client_EmptyEvent())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.NewWrappedDynamicClient","title":"<code>NewWrappedDynamicClient(qps, burst, timeout)</code>","text":"<p>NewWrappedDynamicClient(float qps, int burst, str timeout) object, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def NewWrappedDynamicClient(qps, burst, timeout):\n\t\"\"\"NewWrappedDynamicClient(float qps, int burst, str timeout) object, str\"\"\"\n\treturn WrappedDynamicClient(handle=_wrapper.client_NewWrappedDynamicClient(qps, burst, timeout))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.NewWrappedDynamicClientWithConfig","title":"<code>NewWrappedDynamicClientWithConfig(config, qps, burst, timeout)</code>","text":"<p>NewWrappedDynamicClientWithConfig([]int config, float qps, int burst, str timeout) object, str</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def NewWrappedDynamicClientWithConfig(config, qps, burst, timeout):\n\t\"\"\"NewWrappedDynamicClientWithConfig([]int config, float qps, int burst, str timeout) object, str\"\"\"\n\treturn WrappedDynamicClient(handle=_wrapper.client_NewWrappedDynamicClientWithConfig(config.handle, qps, burst, timeout))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.NewWrappedWatchInterface","title":"<code>NewWrappedWatchInterface(watcher)</code>","text":"<p>NewWrappedWatchInterface(object watcher) object</p> <p>Construct a new WrappedWatchInterface from a client-go watch interface</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def NewWrappedWatchInterface(watcher):\n\t\"\"\"NewWrappedWatchInterface(object watcher) object\n\n\tConstruct a new WrappedWatchInterface from a client-go watch interface\n\t\"\"\"\n\treturn WrappedWatchInterface(handle=_wrapper.client_NewWrappedWatchInterface(watcher.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.client.Set_EmptyEvent","title":"<code>Set_EmptyEvent(value)</code>","text":"<p>Set_EmptyEvent Sets Go Variable: client.EmptyEvent</p> Source code in <code>kubernetes_lite/wrapper/client.py</code> <pre><code>def Set_EmptyEvent(value):\n\t\"\"\"\n\tSet_EmptyEvent Sets Go Variable: client.EmptyEvent\n\n\t\"\"\"\n\tif isinstance(value, go.GoClass):\n\t\t_wrapper.client_Set_EmptyEvent(value.handle)\n\telse:\n\t\t_wrapper.client_Set_EmptyEvent(value)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go","title":"<code>go</code>","text":""},{"location":"api_references/#kubernetes_lite.wrapper.go.GoClass","title":"<code>GoClass</code>","text":"<p>               Bases: <code>object</code></p> <p>GoClass is the base class for all GoPy wrapper classes</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class GoClass(object):\n\t\"\"\"GoClass is the base class for all GoPy wrapper classes\"\"\"\n\tdef __init__(self):\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Ptr_watch_Event","title":"<code>Ptr_watch_Event</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Ptr_watch_Event(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], int):\n\t\t\tself.handle = args[0]\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Ptr_watch_Event.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], int):\n\t\tself.handle = args[0]\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_bool","title":"<code>Slice_bool</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_bool(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_bool_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_bool.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_bool len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_bool([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_bool_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_bool_len(self.handle)\n\t\t\t\treturn Slice_bool(handle=_wrapper.Slice_bool_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_bool_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_bool_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_bool.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_bool_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_bool_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_bool.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_bool_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_bool.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_bool.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_byte","title":"<code>Slice_byte</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_byte(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_byte_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_byte.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_byte len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_byte([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_byte_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_byte_len(self.handle)\n\t\t\t\treturn Slice_byte(handle=_wrapper.Slice_byte_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_byte_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_byte_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_byte.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_byte_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_byte_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n\t@staticmethod\n\tdef from_bytes(value):\n\t\t\"\"\"Create a Go []byte object from a Python bytes object\"\"\"\n\t\thandle = _wrapper.Slice_byte_from_bytes(value)\n\t\treturn Slice_byte(handle=handle)\n\tdef __bytes__(self):\n\t\t\"\"\"Convert the slice to a bytes object.\"\"\"\n\t\treturn _wrapper.Slice_byte_to_bytes(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_byte.__bytes__","title":"<code>__bytes__()</code>","text":"<p>Convert the slice to a bytes object.</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __bytes__(self):\n\t\"\"\"Convert the slice to a bytes object.\"\"\"\n\treturn _wrapper.Slice_byte_to_bytes(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_byte.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_byte_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_byte.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_byte.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_byte.from_bytes","title":"<code>from_bytes(value)</code>  <code>staticmethod</code>","text":"<p>Create a Go []byte object from a Python bytes object</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>@staticmethod\ndef from_bytes(value):\n\t\"\"\"Create a Go []byte object from a Python bytes object\"\"\"\n\thandle = _wrapper.Slice_byte_from_bytes(value)\n\treturn Slice_byte(handle=handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_error","title":"<code>Slice_error</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_error(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_error_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_error.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_error len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_error([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_error_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_error_len(self.handle)\n\t\t\t\treturn Slice_error(handle=_wrapper.Slice_error_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_error_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_error_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_error.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_error_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_error_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_error.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_error_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_error.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_error.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float32","title":"<code>Slice_float32</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_float32(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_float32_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_float32.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_float32 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_float32([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_float32_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_float32_len(self.handle)\n\t\t\t\treturn Slice_float32(handle=_wrapper.Slice_float32_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_float32_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_float32_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_float32.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_float32_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_float32_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float32.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_float32_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_float32.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float32.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float64","title":"<code>Slice_float64</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_float64(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_float64_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_float64.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_float64 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_float64([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_float64_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_float64_len(self.handle)\n\t\t\t\treturn Slice_float64(handle=_wrapper.Slice_float64_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_float64_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_float64_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_float64.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_float64_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_float64_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float64.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_float64_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_float64.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_float64.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int","title":"<code>Slice_int</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_int(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_int_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_int.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_int len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_int([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_int_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_int_len(self.handle)\n\t\t\t\treturn Slice_int(handle=_wrapper.Slice_int_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_int_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_int_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_int.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_int_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_int_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_int_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_int.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int16","title":"<code>Slice_int16</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_int16(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_int16_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_int16.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_int16 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_int16([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_int16_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_int16_len(self.handle)\n\t\t\t\treturn Slice_int16(handle=_wrapper.Slice_int16_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_int16_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_int16_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_int16.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_int16_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_int16_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int16.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_int16_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_int16.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int16.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int32","title":"<code>Slice_int32</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_int32(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_int32_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_int32.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_int32 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_int32([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_int32_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_int32_len(self.handle)\n\t\t\t\treturn Slice_int32(handle=_wrapper.Slice_int32_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_int32_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_int32_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_int32.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_int32_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_int32_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int32.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_int32_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_int32.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int32.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int64","title":"<code>Slice_int64</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_int64(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_int64_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_int64.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_int64 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_int64([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_int64_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_int64_len(self.handle)\n\t\t\t\treturn Slice_int64(handle=_wrapper.Slice_int64_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_int64_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_int64_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_int64.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_int64_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_int64_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int64.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_int64_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_int64.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int64.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int8","title":"<code>Slice_int8</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_int8(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_int8_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_int8.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_int8 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_int8([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_int8_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_int8_len(self.handle)\n\t\t\t\treturn Slice_int8(handle=_wrapper.Slice_int8_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_int8_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_int8_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_int8.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_int8_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_int8_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int8.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_int8_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_int8.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_int8.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_rune","title":"<code>Slice_rune</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_rune(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_rune_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_rune.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_rune len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_rune([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_rune_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_rune_len(self.handle)\n\t\t\t\treturn Slice_rune(handle=_wrapper.Slice_rune_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_rune_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_rune_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_rune.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_rune_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_rune_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_rune.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_rune_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_rune.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_rune.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_string","title":"<code>Slice_string</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_string(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_string_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_string.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_string len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_string([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_string_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_string_len(self.handle)\n\t\t\t\treturn Slice_string(handle=_wrapper.Slice_string_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_string_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_string_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_string.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_string_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_string_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_string.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_string_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_string.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_string.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint","title":"<code>Slice_uint</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_uint(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_uint_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_uint.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_uint len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_uint([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_uint_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_uint_len(self.handle)\n\t\t\t\treturn Slice_uint(handle=_wrapper.Slice_uint_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_uint_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_uint_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_uint.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_uint_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_uint_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_uint_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_uint.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint16","title":"<code>Slice_uint16</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_uint16(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_uint16_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_uint16.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_uint16 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_uint16([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_uint16_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_uint16_len(self.handle)\n\t\t\t\treturn Slice_uint16(handle=_wrapper.Slice_uint16_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_uint16_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_uint16_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_uint16.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_uint16_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_uint16_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint16.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_uint16_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_uint16.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint16.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint32","title":"<code>Slice_uint32</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_uint32(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_uint32_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_uint32.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_uint32 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_uint32([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_uint32_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_uint32_len(self.handle)\n\t\t\t\treturn Slice_uint32(handle=_wrapper.Slice_uint32_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_uint32_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_uint32_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_uint32.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_uint32_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_uint32_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint32.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_uint32_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_uint32.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint32.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint64","title":"<code>Slice_uint64</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_uint64(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_uint64_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_uint64.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_uint64 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_uint64([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_uint64_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_uint64_len(self.handle)\n\t\t\t\treturn Slice_uint64(handle=_wrapper.Slice_uint64_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_uint64_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_uint64_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_uint64.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_uint64_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_uint64_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint64.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_uint64_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_uint64.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint64.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint8","title":"<code>Slice_uint8</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class Slice_uint8(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameter is a python list that we copy from\n\t\t\"\"\"\n\t\tself.index = 0\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.Slice_uint8_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\t\t\tif len(args) &gt; 0:\n\t\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\t\traise TypeError('Slice_uint8.__init__ takes a sequence as argument')\n\t\t\t\tfor elt in args[0]:\n\t\t\t\t\tself.append(elt)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\ts = 'go.Slice_uint8 len: ' + str(len(self)) + ' handle: ' + str(self.handle) + ' ['\n\t\tif len(self) &lt; 120:\n\t\t\ts += ', '.join(map(str, self)) + ']'\n\t\treturn s\n\tdef __repr__(self):\n\t\treturn 'go.Slice_uint8([' + ', '.join(map(str, self)) + '])'\n\tdef __len__(self):\n\t\treturn _wrapper.Slice_uint8_len(self.handle)\n\tdef __getitem__(self, key):\n\t\tif isinstance(key, slice):\n\t\t\tif key.step == None or key.step == 1:\n\t\t\t\tst = key.start\n\t\t\t\ted = key.stop\n\t\t\t\tif st == None:\n\t\t\t\t\tst = 0\n\t\t\t\tif ed == None:\n\t\t\t\t\ted = _wrapper.Slice_uint8_len(self.handle)\n\t\t\t\treturn Slice_uint8(handle=_wrapper.Slice_uint8_subslice(self.handle, st, ed))\n\t\t\treturn [self[ii] for ii in range(*key.indices(len(self)))]\n\t\telif isinstance(key, int):\n\t\t\tif key &lt; 0:\n\t\t\t\tkey += len(self)\n\t\t\tif key &lt; 0 or key &gt;= len(self):\n\t\t\t\traise IndexError('slice index out of range')\n\t\t\treturn _wrapper.Slice_uint8_elem(self.handle, key)\n\t\telse:\n\t\t\traise TypeError('slice index invalid type')\n\tdef __setitem__(self, idx, value):\n\t\tif idx &lt; 0:\n\t\t\tidx += len(self)\n\t\tif idx &lt; len(self):\n\t\t\t_wrapper.Slice_uint8_set(self.handle, idx, value)\n\t\t\treturn\n\t\traise IndexError('slice index out of range')\n\tdef __iadd__(self, value):\n\t\tif not isinstance(value, _collections_abc.Iterable):\n\t\t\traise TypeError('Slice_uint8.__iadd__ takes a sequence as argument')\n\t\tfor elt in value:\n\t\t\tself.append(elt)\n\t\treturn self\n\tdef __iter__(self):\n\t\tself.index = 0\n\t\treturn self\n\tdef __next__(self):\n\t\tif self.index &lt; len(self):\n\t\t\trv = _wrapper.Slice_uint8_elem(self.handle, self.index)\n\t\t\tself.index = self.index + 1\n\t\t\treturn rv\n\t\traise StopIteration\n\tdef append(self, value):\n\t\t_wrapper.Slice_uint8_append(self.handle, value)\n\tdef copy(self, src):\n\t\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\t\tmx = min(len(self), len(src))\n\t\tfor i in range(mx):\n\t\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint8.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameter is a python list that we copy from</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameter is a python list that we copy from\n\t\"\"\"\n\tself.index = 0\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.Slice_uint8_CTor()\n\t\t_wrapper.IncRef(self.handle)\n\t\tif len(args) &gt; 0:\n\t\t\tif not isinstance(args[0], _collections_abc.Iterable):\n\t\t\t\traise TypeError('Slice_uint8.__init__ takes a sequence as argument')\n\t\t\tfor elt in args[0]:\n\t\t\t\tself.append(elt)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Slice_uint8.copy","title":"<code>copy(src)</code>","text":"<p>copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def copy(self, src):\n\t\"\"\" copy emulates the go copy function, copying elements into this list from source list, up to min of size of each list \"\"\"\n\tmx = min(len(self), len(src))\n\tfor i in range(mx):\n\t\tself[i] = src[i]\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.runtime_Object","title":"<code>runtime_Object</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class runtime_Object(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], int):\n\t\t\tself.handle = args[0]\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.runtime_Object.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], int):\n\t\tself.handle = args[0]\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.watch_Event","title":"<code>watch_Event</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class watch_Event(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], int):\n\t\t\tself.handle = args[0]\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.watch_Event.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], int):\n\t\tself.handle = args[0]\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.watch_Interface","title":"<code>watch_Interface</code>","text":"<p>               Bases: <code>GoClass</code></p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>class watch_Interface(GoClass):\n\t\"\"\"\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], int):\n\t\t\tself.handle = args[0]\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.watch_Interface.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], int):\n\t\tself.handle = args[0]\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.go.Init","title":"<code>Init()</code>","text":"<p>calls the GoPyInit function, which runs the 'main' code string that was passed using -main arg to gopy</p> Source code in <code>kubernetes_lite/wrapper/go.py</code> <pre><code>def Init():\n\t\"\"\"calls the GoPyInit function, which runs the 'main' code string that was passed using -main arg to gopy\"\"\"\n\t_wrapper.GoPyInit()\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server","title":"<code>server</code>","text":"<p>----------------------------------------------------------------- * * (C) Copyright IBM Corporation 2024.                               * *                                                                   * * The source code for this program is not published or otherwise    * * divested of its trade secrets, irrespective of what has been      * * deposited with the U.S. Copyright Office.                         * * -----------------------------------------------------------------</p>"},{"location":"api_references/#kubernetes_lite.wrapper.server.DefaultContextName","title":"<code>DefaultContextName = 'default-context'</code>  <code>module-attribute</code>","text":"<p>Default settings for the kube config</p>"},{"location":"api_references/#kubernetes_lite.wrapper.server.DefaultNamespace","title":"<code>DefaultNamespace = 'default'</code>  <code>module-attribute</code>","text":"<p>Default settings for the kube config</p>"},{"location":"api_references/#kubernetes_lite.wrapper.server.EnvTestEnvironment","title":"<code>EnvTestEnvironment</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>EnvTestEnvironment defines the interface for starting and stopping an EnvTest server as well as fetching a generic kube config</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>class EnvTestEnvironment(go.GoClass):\n\t\"\"\"EnvTestEnvironment defines the interface for starting and stopping an EnvTest server as well as\\nfetching a generic kube config\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = 0\n\tdef GetKubeConfig(self):\n\t\t\"\"\"GetKubeConfig() []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.server_EnvTestEnvironment_GetKubeConfig(self.handle))\n\tdef Start(self):\n\t\t\"\"\"Start() []int, str\"\"\"\n\t\treturn go.Slice_byte(handle=_wrapper.server_EnvTestEnvironment_Start(self.handle))\n\tdef Stop(self):\n\t\t\"\"\"Stop() str\"\"\"\n\t\treturn _wrapper.server_EnvTestEnvironment_Stop(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.EnvTestEnvironment.GetKubeConfig","title":"<code>GetKubeConfig()</code>","text":"<p>GetKubeConfig() []int, str</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def GetKubeConfig(self):\n\t\"\"\"GetKubeConfig() []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.server_EnvTestEnvironment_GetKubeConfig(self.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.EnvTestEnvironment.Start","title":"<code>Start()</code>","text":"<p>Start() []int, str</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def Start(self):\n\t\"\"\"Start() []int, str\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.server_EnvTestEnvironment_Start(self.handle))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.EnvTestEnvironment.Stop","title":"<code>Stop()</code>","text":"<p>Stop() str</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def Stop(self):\n\t\"\"\"Stop() str\"\"\"\n\treturn _wrapper.server_EnvTestEnvironment_Stop(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.EnvTestEnvironment.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = 0\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.NewEnvTestEnvironment","title":"<code>NewEnvTestEnvironment()</code>","text":"<p>NewEnvTestEnvironment() object, str</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def NewEnvTestEnvironment():\n\t\"\"\"NewEnvTestEnvironment() object, str\"\"\"\n\treturn EnvTestEnvironment(handle=_wrapper.server_NewEnvTestEnvironment())\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.server.NewEnvTestEnvironmentWithPath","title":"<code>NewEnvTestEnvironmentWithPath(path)</code>","text":"<p>NewEnvTestEnvironmentWithPath(str path) object, str</p> Source code in <code>kubernetes_lite/wrapper/server.py</code> <pre><code>def NewEnvTestEnvironmentWithPath(path):\n\t\"\"\"NewEnvTestEnvironmentWithPath(str path) object, str\"\"\"\n\treturn EnvTestEnvironment(handle=_wrapper.server_NewEnvTestEnvironmentWithPath(path))\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.setup","title":"<code>setup</code>","text":"<p>----------------------------------------------------------------- * * (C) Copyright IBM Corporation 2024.                               * *                                                                   * * The source code for this program is not published or otherwise    * * divested of its trade secrets, irrespective of what has been      * * deposited with the U.S. Copyright Office.                         * * -----------------------------------------------------------------</p> <p>----------------------------------------------------------------- * * (C) Copyright IBM Corporation 2024.                               * *                                                                   * * The source code for this program is not published or otherwise    * * divested of its trade secrets, irrespective of what has been      * * deposited with the U.S. Copyright Office.                         * * -----------------------------------------------------------------</p>"},{"location":"api_references/#kubernetes_lite.wrapper.setup.SetupEnvTestResult","title":"<code>SetupEnvTestResult</code>","text":"<p>               Bases: <code>GoClass</code></p> <p>SetupEnvTestResult contains the various results from running the SetupEnvTest command includes stdout/stderr and the serialized error message</p> Source code in <code>kubernetes_lite/wrapper/setup.py</code> <pre><code>class SetupEnvTestResult(go.GoClass):\n\t\"\"\"SetupEnvTestResult contains the various results from running the SetupEnvTest command\\nincludes stdout/stderr and the serialized error message\\n\"\"\"\n\tdef __init__(self, *args, **kwargs):\n\t\t\"\"\"\n\t\thandle=A Go-side object is always initialized with an explicit handle=arg\n\t\totherwise parameters can be unnamed in order of field names or named fields\n\t\tin which case a new Go object is constructed first\n\t\t\"\"\"\n\t\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\t\tself.handle = kwargs['handle']\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\t\tself.handle = args[0].handle\n\t\t\t_wrapper.IncRef(self.handle)\n\t\telse:\n\t\t\tself.handle = _wrapper.setup_SetupEnvTestResult_CTor()\n\t\t\t_wrapper.IncRef(self.handle)\n\tdef __del__(self):\n\t\t_wrapper.DecRef(self.handle)\n\tdef __str__(self):\n\t\tpr = [(p, getattr(self, p)) for p in dir(self) if not p.startswith('__')]\n\t\tsv = 'setup.SetupEnvTestResult{'\n\t\tfirst = True\n\t\tfor v in pr:\n\t\t\tif callable(v[1]):\n\t\t\t\tcontinue\n\t\t\tif first:\n\t\t\t\tfirst = False\n\t\t\telse:\n\t\t\t\tsv += ', '\n\t\t\tsv += v[0] + '=' + str(v[1])\n\t\treturn sv + '}'\n\tdef __repr__(self):\n\t\tpr = [(p, getattr(self, p)) for p in dir(self) if not p.startswith('__')]\n\t\tsv = 'setup.SetupEnvTestResult ( '\n\t\tfor v in pr:\n\t\t\tif not callable(v[1]):\n\t\t\t\tsv += v[0] + '=' + str(v[1]) + ', '\n\t\treturn sv + ')'\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.setup.SetupEnvTestResult.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>handle=A Go-side object is always initialized with an explicit handle=arg otherwise parameters can be unnamed in order of field names or named fields in which case a new Go object is constructed first</p> Source code in <code>kubernetes_lite/wrapper/setup.py</code> <pre><code>def __init__(self, *args, **kwargs):\n\t\"\"\"\n\thandle=A Go-side object is always initialized with an explicit handle=arg\n\totherwise parameters can be unnamed in order of field names or named fields\n\tin which case a new Go object is constructed first\n\t\"\"\"\n\tif len(kwargs) == 1 and 'handle' in kwargs:\n\t\tself.handle = kwargs['handle']\n\t\t_wrapper.IncRef(self.handle)\n\telif len(args) == 1 and isinstance(args[0], go.GoClass):\n\t\tself.handle = args[0].handle\n\t\t_wrapper.IncRef(self.handle)\n\telse:\n\t\tself.handle = _wrapper.setup_SetupEnvTestResult_CTor()\n\t\t_wrapper.IncRef(self.handle)\n</code></pre>"},{"location":"api_references/#kubernetes_lite.wrapper.setup.SetupEnvTest","title":"<code>SetupEnvTest(stdin_data, arg_data)</code>","text":"<p>SetupEnvTest([]int stdin_data, []int arg_data) []int, str</p> <p>SetupEnvTest runs the vendored main file with the provided stdin_data and json arg data</p> Source code in <code>kubernetes_lite/wrapper/setup.py</code> <pre><code>def SetupEnvTest(stdin_data, arg_data):\n\t\"\"\"SetupEnvTest([]int stdin_data, []int arg_data) []int, str\n\n\tSetupEnvTest runs the vendored main file with the provided stdin_data and json arg data\n\t\"\"\"\n\treturn go.Slice_byte(handle=_wrapper.setup_SetupEnvTest(stdin_data.handle, arg_data.handle))\n</code></pre>"}]}